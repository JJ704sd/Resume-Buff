# AGENTS.md

个人简历助手 — 一份素材库，一键生成多份针对性简历。本地单用户工具。

> 本仓库服务于**本地开发**：`PUT /api/materials` 无鉴权，**不要直接暴露公网**。

## Setup commands

- 后端安装依赖：`cd backend && pip install -r requirements.txt`
- 后端启动：     `cd backend && python main.py` （http://127.0.0.1:8000）
- 前端安装依赖：`cd frontend && npm install`
- 前端启动：     `cd frontend && npm run dev` （http://127.0.0.1:5173）
- 前端构建：     `cd frontend && npm run build` （产物到 `frontend/dist/`，已在 .gitignore）
- 安装 pre-push hook：`powershell -ExecutionPolicy Bypass -File scripts/install-hooks.ps1`
- 跳过 hook：`git push --no-verify`（紧急情况）
- 前端类型检查： `cd frontend && npx vue-tsc --noEmit`

## Project layout

- `backend/` — Python FastAPI 后端
  - `main.py` — FastAPI 入口与 CORS 配置
  - `api/materials.py` — 素材库 CRUD（`/api/materials`）
  - `api/resume.py` — 预览 / 生成 / 角色列表（`/api/resume`）
  - `core/generator.py` — sections 构造 + python-docx 渲染（**核心域**）
  - `core/agent_tools.py` — Agent 工具注册表 + 统一执行入口(R5-A Phase 1)
  - `core/agent_workflow.py` — 受控 Plan-and-Execute 编排器(R5-A Phase 1)
  - `core/logger.py` — `backend/logs/generation.log` 写入
  - `data/materials.json` — 素材库（**单人唯一真源**，已脱敏）
  - `output/`、`logs/` — 运行时产物（`.gitignore`，本地保留）
- `frontend/` — Vue 3 + TypeScript + Vite 单页
  - `src/App.vue` — 三段式主界面（选岗位 → 预览 → 下载）+ ② JD 评分卡
  - `src/api/index.ts` — axios 封装（`materialsApi` / `resumeApi` / `jdApi`）
  - `vite.config.ts` — `/api` 代理到 `:8000`
- `.harness/` — 多 agent 协作脚手架（`agent.md` = orchestrator；`reins/{developer,tester}/` = 两个 rein；`docs/` = 架构/流程/隐私；`memory/` = 团队共享记忆）
- `简历帮知识库/` — 个人素材草稿（`.gitignore`，不进库）

## AI 岗位 JD 资料库（v4 实习岗筛选版，2026-06-27，最近更新 2026-06-27 Round 3.5+）

> 个人用的 AI 行业 JD 池子，与 `backend/core/jd_parser.py` 的 `match_score` 联动 — 投递前先把目标 JD 全文跑 `match_score(text, role, materials)`，看素材库匹配度。

顶层 3 个文件（**只保留最新版**，旧版本已清理到回收站）：
- `AI岗位JD库_v4_intern.json` — **主库**：86 份 JD（v3 42 + 补搜实习岗 40 + Round 3.5 百运网 4），每份带 `intern_match` 4 级标签（strong / campus_to_intern / weak / none）
- `AI岗位JD库_v4_intern_筛选报告.md` — **筛选报告**：4 级规则 + 52 份实习可投清单 + 公司维度统计
- `AI岗位JD库_v4_黄金标的match报告.md` — **match_score 实测**：JD-B010 字节 / JD-D007 DeepSeek / JD-A012 阿里 3 份黄金 × 6 role = 18 次匹配对比 + 局限性说明

> R3.5 阈值调优 ground truth 如需独立保存,后续可在 `简历帮知识库/jd_samples.json` 单独建文件(含 label / label_note),不污染主库

关联 `scripts/`：
- `scripts/build_v4.py` — v4 入库脚本（v3 + 补搜 JD + 后续扩库，含 ID 去重）
- `scripts/score_intern_match.py` — 4 级实习匹配打标 + Markdown 报告生成
- `scripts/match_golden_targets.py` — 黄金 JD × 6 role 的 match_score 实测

扩库流程：
1. 在 `build_v4.py` 的 `NEW_JDS` 列表追加新 JD（注意 ID 不能跟现有 86 个重复，命名沿用 `JD-X###`，公司用首字母缩写如 BY=百运网）
2. `python scripts/build_v4.py` → 重建 `AI岗位JD库_v4_intern.json`
3. `python scripts/score_intern_match.py` → 重打 4 级标签 + 生成筛选报告
4. 旧版本文件直接 trash，**不保留 v1/v2/v3 历史**（避免目录冗余）
5. 如果是手工抄录的 JD（非网上抓），`source_url` 填 `"user_provided:<来源描述>"`、`source_quality` 用 `"unverified"`、`authenticity` 分打低分（如 18 分）

## Code style

- Python：PEP 8，类型注解推荐但不强求；函数文档串用中文说明业务含义
- TypeScript：`tsconfig.json` 启用 `strict: true`；Vue SFC 用 `<script setup lang="ts">`
- 缩进统一 2 空格；后端不用 formatter，前端不加 ESLint/Prettier 配置（沿用 Vite 默认）
- 命名：snake_case（Python）/ camelCase（TS）；role id 用 snake_case（如 `tech_metric`）

## Testing instructions

  - 后端 `pytest`：R6-G 起活跃基线 **948 个用例**(2026-07-03,本 round `feat(round6-g): review-needed 整理 — verifier sentinel + envelope hygiene + stderr 脱敏` 落地后 `D:\python3.11\python.exe -m pytest tests/ -q` 实测 948 passed in 117.94s);计算口径沿用 R6-E 936 + R6-G +12(R6-F closeout docs-only patch 不引入新测试,R6-G 落地 3 review-needed):`backend/core/interview_verifier.py` 加 `INTERVIEW_VERIFIER_FAIL_WARNING` sentinel 常量 + `verify_draft_card` 在 unsupported/low_confidence>0 时返 sentinel 提示(否则 UI 看到 verifier 崩了会误判"全 verified 通过" — F-2.1)+ `backend/core/interview_llm.py` envelope except hygiene 清理(`except (ValueError, OSError, Exception)` 冗余简化,行为不变 — F-2.2)+ `scripts/evaluate_interview_agent.py` stderr 错误信息脱 `LLM_API_KEY` 字面量 / `sk-` 前缀 / `Bearer` 头(R5-D spec §6.4 严格口子 — F-2.3);= **948 passed + 0 skipped**(R6-G 起)— **当前活跃基线以本轮 R6-K Postman closeout 989 为准**(R6-J 扩 eval set +11 → 959,R6-K 实施 +14 → 973,R6-K Postman closeout +16 → 989;0 回归);历史 round 描述里的 683 是 R5-E 收尾时快照,723/729/739/768/809/840/863/877/889/909/930/936/948/959/973 见各 round entry
  - **R6-C.1 起:eval contract warnings + live compare wording fix 锁死(2026-07-01 落地)** —— 路线 A 实施(round6-c-live-eval-result-and-next-steps §5);`scripts/evaluate_interview_agent.py` 新增 `_validate_eval_contract(sample)` 纯函数: 校验 `expected_slots` 是否在对应 gap 的 `GAP_SUGGESTED_SLOTS` 内 + 在 `MAX_TURNS_PER_GAP=3` 范围内可达;near_limit 触达集合 `{metric, result}` 跳过 beyond 检查;2 个新 warning code 常量 `EVAL_CONTRACT_WARN_UNREACHABLE` / `EVAL_CONTRACT_WARN_BEYOND_3`;`write_report` 新增 `## 4.5 Eval contract warnings` 章节,按 sample 去重(compare 模式双组同跑不重复),只列 sample/gap/slot/code 4 字段不含 user_message;修正 live compare 表头 stale wording(按 requested_mode 动态化: live → "live + 已配置 LLM 凭据 → 真实 LLM 抽取",offline → "offline → 强制规则 fallback");`fallback_rate` 口径声明明确为 workflow/session 级,不是 slot 级 LLM 成功率;**14 个 R6-C.1 pytest** = TestPhaseC1EvalContractValidation 7(unreachable / beyond / near_limit 跳过 / 可达无 warning / 空 expected / 未知 gap / record 4 字段)+ TestPhaseC1ReportContractSection 2(章节存在 + 渲染 sample/gap/slot/code)+ TestPhaseC1ReportWording 3(live 无 stale wording / offline 保留 fallback label / fallback_rate 口径声明)+ TestPhaseC1ReportPrivacy 2(无 user_message / source_span / API key / prompt 正文);baseline 863 → **877 全绿**(14 新增, 863 老测试零回退);**不挂 pre-push hook**;**不修改** `core/` / `evaluate_agent_workflow.py` / `evaluate_prompt_versions.py` / 任何前端
  - **R6-C.2A 起:fix eval set contract — adjust communication_club + add product_goal per sample 锁死(2026-07-01 落地)** —— 路线 A 收尾, **不改 policy**;**核心边界** — `scripts/evaluate_interview_agent.py` 中 10 条 eval sample 各加 `product_goal` (枚举 `three_turn_friendly` / `full_fact_coverage`) + `contract_note` (str, 合同决策说明) 2 个字段;`communication_club` (plan_baseline) 调整 `expected_slots`: 移除 `responsibility` (不在 `communication` suggested), 改为 `(action, method, result)` 表达 "3 轮内可生成素材" 目标;7 条 simulated samples **不删 expected** (符合"完整项目事实覆盖"目标), 保留含 unreachable / beyond 的 slot, 在 `contract_note` 字段标记 "需后续 policy 调整" (e.g. `sim_tech_metric_medical_eval` 的 metric 不在 `tech_metric` suggested / `sim_tech_metric_ecg` 的 method 在 position 3 + metric 不在 suggested / `sim_communication_volunteer` 的 responsibility 不在 `communication` suggested / `sim_tech_metric_rubric_design` 同 ecg);`write_report` 新增 `## 4.6 Eval contract: product goal` 章节,按 sample 去重列表展示 product_goal / 合同说明, 配合 4.5 章节共同支撑 `schema_pass_rate` 解读;§八 结论追加 "schema_pass_rate 数值变化 = 评测合同变化" 口径声明;**验收口径** (R6-C.2A 强制) — `schema_pass_rate` 数值变化必须解读为 **评测合同变化** (`expected_slots` 调整 / `product_goal` 标记), **不**解读为 LLM 抽取能力提升或下降;**禁止**把 `expected_slots` 改成当前 captured slot keys (作弊) — `communication_club` 改后 expected `(action, method, result)` 含 result (captured 没), 证明是合同驱动不是结果驱动, 测试 `test_communication_club_removed_responsibility_slot` 锁死;**`product_goal` 分工** — `plan_baseline` (3 条) 标 `three_turn_friendly` (3 轮内可生成素材, 调整后 0 warning), `simulated_user_v1` (7 条) 标 `full_fact_coverage` (完整项目事实覆盖, 保留 expected, 6 条 unique warning 跨 4 条 sample);**12 个 R6-C.2A pytest** (见基线段落)+ baseline 877 → **889 全绿** (877 老测试零回退);**不挂 pre-push hook**;**不修改** `core/` / `core/interview_agent.py` / `core/interview_prompts.py` / `core/interview_policy.py` / `core/interview_verifier.py` / 任何前端;**offline 跑**实测 — `total=10 schema_pass_rate=0.30 avg_completeness=0.53 fabric_violations=0 low_confidence_slot_rate=0.23` (跟 R6-C.1 baseline 一致, 0.30 不变但内部 fail 原因变了 — 合同驱动);**R6-C.2A 不**做 — 自动切默认 product_goal / 删除 unreachable warning 章节 / 真正扩 policy 把 metric 加进 tech_metric suggested (留后续 round) / 把 simulated 样本缩到 3-5 条 (留 R6-D 决策);**R6-C 后续候选** — (a) R6-D: 根据 contract warning 列表的统计决定是否要 policy 改动 (扩 near_limit / 扩 suggested 集合) 还是先扩 eval set 到 20+ 条 (更多 boundary sample) (b) R6-E: 用户跑 10+ 轮真实对话后跑 live compare v2 报告作 R6-A Phase 4 LLM 抽取收益决策依据
  - **R6-C.2B 起:policy gap-specific critical slot 补足锁死(2026-07-01 落地)** —— 路线 B 收尾(R6-C.2A 路线 A 不改 policy,本轮路线 B 改 policy 让 critical slot 三轮内可达);**核心边界** — 只改 `backend/core/interview_policy.py` + `backend/tests/test_interview_policy.py` + `backend/tests/test_interview_agent.py` 3 个文件;不修改 `core/interview_agent.py` 的 LLM 抽取逻辑 / `core/interview_prompts.py` (GAP_SUGGESTED_SLOTS 是 LLM 抽取端引导顺序, 改它会污染 LLM 抽取链路);不修改 frontend / 不提交 live report;**改动内容** — `INTERVIEW_POLICY_GAP_CRITICAL_SLOTS` 新常量 2 entries(`tech_metric: ("metric", "method")` + `communication: ("responsibility",)`)+ `INTERVIEW_POLICY_REASON_GAP_CRITICAL_SLOT = "gap_critical_slot_priority"` 新 reason_code + `_find_missing_critical_slots(session)` 新 helper 纯 dict lookup 返未 captured 列表(配置顺序保留) + `plan_next_question` 新增 step 4.5 优先级位置:**优先级链中 step 4.5 在 step 4 (low_confidence) 之后, step 5 (near_limit) 之前**, 触发条件 = 当前 gap 在 `INTERVIEW_POLICY_GAP_CRITICAL_SLOTS` 有配置 + 有未 captured 的 critical slot;**与 R6-C.1 contract warning 6 条对应的修复** — `sim_tech_metric_medical_eval` 的 metric / `sim_tech_metric_ecg` 的 metric+method / `sim_tech_metric_rubric_design` 的 metric+method / `sim_communication_volunteer` 的 responsibility 现在能通过 step 4.5 在三轮内被问到(对应 5 条 unreachable/beyond warning, 剩 1 条 `sim_tech_metric_ecg` 的 method at position 3 也属 critical 列表, 现可问);**20 个 R6-C.2B pytest** = `tests/test_interview_policy.py`(TestPhaseC2BCriticalSlot 16 case: 2 critical priority 路径 — metric_asked / method_after_metric_asked / communication_responsibility_asked;2 优先级位置 — priority_over_near_limit / priority_over_next_suggested;4 step 4.5 不触发边界 — does_not_fire_when_step3_missing / does_not_fire_when_step4_low_confidence / does_not_fire_when_skip_limit / does_not_fire_when_turn_limit;2 critical captured / gap not in registry — does_not_fire_when_critical_already_captured / does_not_fire_for_gap_not_in_critical_registry;2 配置稳定性 — priority_order_tech_metric / gap_critical_slots_registry_alignment(R6-C.1 contract warning 配置锁死, 防回潮);3 schema 稳定性 — returns_ask_plan_with_correct_kind / plan_includes_low_confidence_slots_aggregation / low_confidence_slots_includes_unrelated_low_conf;1 已有 reason_codes_are_unique 注册新 code = 16 净增) + `tests/test_interview_agent.py`(TestPhaseC2BCriticalSlotIntegration 4 case: tech_metric_prioritizes_metric / communication_prioritizes_responsibility / step_4_5_does_not_break_combo1_chain(3 场景: combo1 空 → step 3 / combo1 只缺 action → step 3 / combo1 满 → step 4.5)+ step_4_5_writes_message_log_for_anti_repeat(verify message_log 写入 1 条 asked entry));baseline 889 → **909 全绿**(20 新增, 889 老测试零回退);**实测** — `D:\python3.11\python.exe -m pytest tests/test_interview_policy.py tests/test_interview_agent.py -q` → 115 passed in 0.38s;`D:\python3.11\python.exe -m pytest tests/ -q` → 909 passed in 61.37s;`scripts/evaluate_interview_agent.py --mode offline` 仍跑 `total=10 schema_pass_rate=0.30 avg_completeness=0.53`(R6-C.2B **不期望** schema_pass_rate 提升 — eval 走 `_extract_slots_iteratively` 路径(基于 `_current_slot` 按 gap.suggested_slots 顺序)不走 policy, 改动只影响 `next_question` 路径(前端 chat panel UI));**不挂 pre-push hook**;**不引入新依赖**(纯 stdlib);**不修改** `core/interview_agent.py` / `core/interview_prompts.py` / 任何 `scripts/` / frontend / eval set;**R6-C.2B 不**做 — 改 GAP_SUGGESTED_SLOTS / 改 CAN_DRAFT_CONDITIONS / 扩 MAX_TURNS_PER_GAP / 切默认 product_goal / 跑 live eval 报告 / 真扩 policy 删 unreachable warning;**R6-C 后续候选** — (a) R6-D: 让前端用户在 chat panel 真实跑 10+ 轮对话验证 step 4.5 的可问性 + 真实 LLM 抽取链路配合 / (b) R6-E: live compare v2 报告作 Phase 5 收益决策依据
  - **R6-C.3 起:LLM 抽取可观测性 + prompt few-shot 优化 锁死(2026-07-02 落地)** —— R6-C.2 阶段收尾增量(不拆模块, 4 个目标一次性交付);**核心边界** — 改 3 个 backend 文件 + 2 个测试文件:`backend/core/interview_prompts.py` / `backend/core/interview_agent.py` / `scripts/evaluate_interview_agent.py` / `backend/tests/test_interview_agent.py` / `backend/tests/test_interview_eval.py`;**不修改** `core/interview_policy.py` (R6-C.2B 路线 B 锁定) / `core/interview_verifier.py` (R6-B Phase 4 锁定) / `core/llm_rewriter.py` / 任何 frontend;**4 个目标落地** — (1) `_call_llm_for_slot_extraction` request body 加 `response_format={"type": "json_object"}` 字段(在 messages 后 / temperature 前, OpenAI-compatible 端点强约束 JSON 输出, 降低 JSON parse retry 概率);temperature 仍 0.0(spec §4.4 字节级一致);(2) `SLOT_EXTRACTION_SYSTEM_PROMPT` 加 2 个短 few-shot 示例(覆盖 string slot `responsibility` + list slot `action`, 例子用脱敏描述 "数据标注 / 看样例 / 判断标准 / 边界", **不**含 JD 原文, 遵守 spec §4.4 模板不含 JD 全文隐私边界);(3) `InterviewSession` 新增 3 个可观测性字段(全部含默认值, 老测试构造兼容):`slot_source_breakdown: dict[str, int]` (rules / llm / mixed 计数, 每轮 answer 后 +1, 老路径 rules-only, LLM 成功 +llm, fallback 算 rules) + `llm_parse_retry_count: int` (累计 JSON parse / schema retry 次数, 网络错不 retry 故不计入) + `llm_to_rules_slot_fallback_count: int` (LLM 失败 fallback 规则版累计次数, 网络 + JSON + schema 错都算);`extract_slots` 接受 `session` 参数(老签名已存在但 unused, 激活), 内部根据 LLM 成功/失败/retry 路径写 3 字段;`_extract_slots_via_llm` 新增 `session` keyword-only 参数, retry 时 `session.llm_parse_retry_count += 1`;`_do_answer` 透传 `sess` 给 `extract_slots` 让 3 字段累计生效;(4) `scripts/evaluate_interview_agent.py` 扩展可观测性 4 链路:`EvalRow` 加 3 字段 (defensive copy 过滤未知 key / 非 int / bool);`compute_metrics` 加 3 聚合 (`slot_source_breakdown_total` / `llm_parse_retry_count_total` / `llm_to_rules_slot_fallback_count_total`) + by_source 拆分 + by_extractor 拆分(compare 模式);`write_report` 新增 `## 4.7、LLM 抽取可观测性 (R6-C.3)` 章节(全局聚合表 + 按 source 拆 + 按 extractor 拆 + 隐私边界声明);每行 sample 摘要加 `src=[rules=N/llm=M/mixed=K] retries=N fb_to_rules=N` 3 字段;§八 结论追加 R6-C.3 LLM 抽取可观测性 1 行;§七 隐私检查加 1 行 "LLM 抽取可观测性只含 slot key + 整数计数 + 比率, 不含原文 (R6-C.3 保护)";`VERSION` 常量更新为 "R6-C.3 (LLM 抽取可观测性 + prompt few-shot; rules/llm/compare 三模式)";**隐私边界 (R6-C.3)** — 3 字段只含整数 / 短字符串("rules" / "llm" / "mixed" 三个字面量), **绝不**含 user_message / source_span / draft_card / API key / prompt 正文 / raw response;EvalRow 在 `_evaluate_one` 末尾 defensive copy + 只取已知 key + int 校验, 防 session 字段污染;测试 `test_report_observability_no_user_message_leak` 抽样 10 个 sentinel 短语 + `test_report_observability_no_prompt_no_source_span_no_api_key` 验证 6 个 forbidden 子串(Few-shot 示例 / sha256: / Bearer / sk- / LLM_API_KEY / BEGIN PROMPT) 不出现在报告里;**21 个 R6-C.3 pytest** (见基线段落)+ baseline 909 → **930 全绿** (909 老测试零回退, 21 新增, 85.78s 跑完);**实测** — `D:\python3.11\python.exe -m pytest tests/ -q` → 930 passed in 85.78s;`D:\python3.11\python.exe -m pytest tests/test_interview_agent.py tests/test_interview_eval.py -q` → 146 passed in 2.22s;前端 `npx vue-tsc --noEmit` 0 error + `npm run build` 成功(`dist/index.html 0.48kB / css 369kB / js 1104kB`);**offline 跑**实测 — `D:\python3.11\python.exe -m scripts.evaluate_interview_agent --mode offline --extractor compare --output backend/logs/interview_eval_report_r6c3.md` → `total=20 (rules=10 + llm 意图=10) rules schema_pass=0.30 llm schema_pass=0.30 llm fallback_rate=1.00`;报告 §4.7 章节渲染: `slot_source_breakdown.rules=60 llm=0 mixed=0 llm_rate=0.0 retries=0 fb_to_rules=0`(60 = 30 rules × 2 组 compare, 符合 offline 模式 LLM 不发网络预期);**不挂 pre-push hook**;**不引入新依赖**(纯 stdlib + dataclass + 已有 `urllib` + `json` + 已有 dict 操作);**不引入新 LLM 调用**(`_call_llm_for_slot_extraction` 复用 R6-A Phase 4 实现, 仅加 `response_format` 字段);**不破坏** R5-E / R6-B / R6-C.1 / R6-C.2 任何边界(老路径 `llm_enabled=False` 字节级一致, 3 字段保持 0 / {} / {}, 因为 `_extract_slots_by_rules` 不写 LLM 可观测字段);**R6-C.3 不**做 — 扩 policy / 改 PROMPT_VERSIONS 把 SLOT_EXTRACTION_SYSTEM_PROMPT 入注册表(决策点 D5 保持) / 改 GAP_SUGGESTED_SLOTS / 改前端  / 跑 live eval 报告自动入库 / 真实 LLM key 接入(留后续 R6-D / R6-E)**) — **当前活跃基线以本轮 R6-D 930 为准**

  - **R6-D 起:机械拆分 LLM slot 抽取模块 锁死(2026-07-02 落地)** —— R6-C.3 之后行为不变重构(spec §5.3 + plan §4.4);**核心边界** — 改 1 个 backend 文件 + 新建 1 个 backend 文件 + 新建 1 个 backend 测试文件 + 改 1 个 backend 测试文件 + 改 1 个 api 测试文件:`backend/core/interview_agent.py`(从 2184 行减到 ~1700 行) + `backend/core/interview_llm.py` (新建, ~430 行, 含 R6-A Phase 4 LLM slot 抽取 + R6-B Phase 1 slot_meta helper + R6-B Phase 2 mode 决策 + R6-C.3 可观测性 schema 全部符号) + `backend/tests/test_interview_agent.py`(从 2854 行减到 1410 行, 删 6 个 LLM class) + `backend/tests/test_interview_llm.py`(新建, 1317 行, 平移 6 个 LLM class 共 35 case) + `backend/tests/test_interview_api.py`(3 处 monkeypatch 路径改 `core.interview_llm` 命名空间);**不修改** `core/interview_policy.py` (R6-C.2B 路线 B 锁定) / `core/interview_verifier.py` (R6-B Phase 4 锁定) / `core/llm_rewriter.py` / `core/interview_prompts.py` / `scripts/` / 任何 frontend / `evaluate_*`;**搬运符号清单** (7 函数 + 3 helper + 5 常量 + 2 mode 常量 + 1 schema dict) — `_resolve_interview_llm_config` / `_validate_llm_extraction_payload` / `_call_llm_for_slot_extraction` / `_try_parse_llm_content` / `_extract_slots_via_llm` / `_attach_llm_slot_meta` / `_decide_interview_mode` / `_has_llm_api_key` / `_validate_confidence` / `_compute_source_span_hash` / `_make_slot_meta` + `_INTERVIEW_LLM_DEFAULT_BASE_URL` / `_INTERVIEW_LLM_DEFAULT_MODEL` / `INTERVIEW_LLM_NO_KEY_WARNING` / `INTERVIEW_MODE_RULES` / `INTERVIEW_MODE_LLM_ASSISTED` / `INTERVIEW_SLOT_META_MIN_CONFIDENCE` / `INTERVIEW_SLOT_META_MAX_CONFIDENCE` / `INTERVIEW_SLOT_META_RULES_CONFIDENCE_FALLBACK` / `INTERVIEW_SLOT_META_LLM_DEFAULT_CONFIDENCE` / `INTERVIEW_OBSERVABILITY_SCHEMA`;**重导出策略** — `interview_agent.py` 通过 `from core.interview_llm import ...` 把全部上述符号 import 进自己命名空间,保持向后兼容,测试和 scripts 仍可 `from core.interview_agent import _attach_llm_slot_meta`;**反向依赖禁止** — `interview_llm.py` 不得 import `core.interview_agent`(仅 `TYPE_CHECKING` 块 import `InterviewSession` 类型注解, 防循环依赖);**反向依赖禁止** — `interview_llm.py` 不得 import `core.llm_rewriter` (R5-E 边界保持,文件任意位置不能出现 `from core.llm_rewriter import ...`);**行为不变保证** — 老路径(llm_enabled=False / 默认)字节级一致, 3 个可观测字段保持 0 / {} / {}, `_extract_slots_by_rules` 不走 LLM 模块任何函数;**mock 路径变更规范** — 测试 mock `urllib.request.urlopen` 必须从 `core.interview_agent.urllib.request.urlopen` 改为 `core.interview_llm.urllib.request.urlopen`(urlopen 在 llm 模块命名空间里被调, monkeypatch 必须改对模块);同理 `monkeypatch.setattr(interview_agent, "_call_llm_for_slot_extraction", ...)` 改为 `setattr(interview_llm, ...)`;**平移测试清单**(35 case 全部从 test_interview_agent 平移到 test_interview_llm, mock 路径同步更新) — `TestLLMSlotExtraction` 6 case (R6-A Phase 4) + `TestInterviewPromptRegistry` 4 case + `TestSlotMetaLlmR6B` 5 case + `TestSlotMetaPrivacyR6B` 3 case + `TestSlotMetaUnitR6B` 6 case + `TestPhaseC3LLMObservability` 11 case;**保留在 test_interview_agent** — `TestSessionLifecycle` / `TestGapSelection` / `TestSlotExtractionRules`(规则版) / `TestDraftCard` / `TestStateMachine` / `TestTracePrivacy` / `TestSaveCard` / `TestSessionDefaultFieldsR6B` / `TestSlotMetaRulesR6B`(规则版 slot_meta) / `TestPhase3PolicyIntegration` / `TestPhaseC2BCriticalSlotIntegration`;**0 新 pytest** —— R6-D 不引入新测试, 全部是行为不变机械平移;实测 `D:\python3.11\python.exe -m pytest tests/ -q` → **930 passed in 87.45s**(R6-C.3 baseline 零回退);R6-C.2B 老路径字节级一致, R6-B 4 phases 锁定的 4 个 file 边界(`core/interview_policy.py` / `core/interview_verifier.py` / `core/interview_agent.py` 老路径 / frontend)全部保持;**不挂 pre-push hook**;**不引入新依赖**(纯 stdlib + TYPE_CHECKING);**不引入新 LLM 调用**;**R6-D 不**做 — 改 LLM prompt 内容 / 改 retry 策略 / 扩 token 控制 / 引入缓存 / 切默认 prompt_version / 改 schema 限制 / 跑 live eval 报告(全部留 R6-D+ / R6-E prompt 优化轮次);**R6-D 后 next round 候选** — (a) R6-D+: LLM prompt 优化(根据真实对话数据迭代 few-shot + temperature 微调, 接入真实 LLM key 后跑 live compare v2)(b) R6-D+: retry 策略升级(指数 backoff / multi-sample / best-of-N)(c) R6-D+: token 成本控制(prompt 压缩 / 缓存命中检测)


  - **R6-E Phase 1 起:文档一致性修复 锁死(2026-07-02 落地)** —— 来自 `.harness/docs/round6-e-github-sync-live-eval-v2-spec.md` §4 Phase 1 实施;**核心边界** — 只改文档不改代码:`.harness/docs/round6-e-github-sync-live-eval-v2-spec.md`(新建 422 行 R6-E 整轮策略文档) + `README.md` 顶部'当前功能基线' + '后端测试基线' + 'scripts 列表' 3 处同步到 R6-C.1+C.2A+C.2B+C.3 + R6-D + 930 baseline + `core/interview_llm.py` 模块拆分 + R6-C.1/2A/3 report 新章节 + `.harness/memory/MEMORY.md` 加 5 条 R6-C/R6-D 里程碑 + R6-E 起步 entry;**不修改** `backend/core/` / `backend/api/` / `frontend/src/` / `scripts/`;**不挂 pre-push hook**;**0 新 pytest** — 纯文档同步;验收命令 `Select-String -LiteralPath README.md, .harness\memory\MEMORY.md -Pattern "863|R6-B Phase 0+1+2+3+4+5+6"` 不再把 863 当活跃基线(spec §4 Phase 1 验收点);**R6-E 完整范围** — Phase 0 取证 ✅ / Phase 1 文档 ✅ / Phase 2 本地验证(待跑 930 pytest + vue-tsc + build + offline compare + 隐私扫描) / Phase 3 GitHub 同步(需用户明确授权) / Phase 4 前端 10+ 轮真实对话 / Phase 5 live eval v2(`--mode live --extractor compare` 出 `interview_eval_report_live_v2.md`) / Phase 6 决策记录(LLM 优启动 R6-F / 持平保持 rules 优先扩 eval set / fallback 高暂停 LLM 修边界);**通过门槛** = `fabrication_violations_count == 0` + live `fallback_rate <= 0.20` + `slot_source_breakdown.llm > 0` + schema_pass / avg_completeness 明确正收益 + 隐私扫描无命中;**强门槛** = schema_pass_rate >= 0.60 + avg_completeness >= 0.70 + fabrication == 0 + 用户主观确认
  - **R6-E Phase 4 起:_do_answer slot 优先读 question_plan 决策 锁死(2026-07-02 落地)** —— Bug fix(commit `7fe798c`),验证发现的真实问题(commit 时未在 spec §6 范围内,但符合 §6 "除非验证发现真实 bug" 例外条款);**Bug 描述** — `_do_answer()` 用 `_current_slot(sess)` 选 slot,该 helper 按 `gap.suggested_slots` 顺序跳过已 captured。但 `next_question()` 调 `plan_next_question`(spec §6 优先级链)按 `CAN_DRAFT_CONDITIONS` 缺啥问啥。两者不对齐,导致 policy 决策的 slot 永远抽不进对应字段,combo 永不满足,`/draft` 端点 400。**复现路径** — communication gap(suggested = background, action, method, result)走 3 轮对答,第 1 轮 policy 问 background,第 2 轮问 action,第 3 轮 policy 知道 combo1 缺 result 所以 question_plan.slot = result。但 _current_slot 按 suggested 顺序跳过 bg + action 返回 method,extract_slots 把用户答的 result 内容塞进 method slot,captured["result"] 永远为空,combo1 永不满足,can_draft=False,/draft 端点返 400。**修复路径(最小侵入,只改 `backend/core/interview_agent.py::_do_answer`)** — (1) 优先读 sess.question_plan["slot"](policy 决策结果) / (2) fallback 到 _current_slot(sess)(spec 兼容: 老测试 / 非 policy 路径仍能走) / (3) 再 fallback 到 ""(extract_slots 在未知 slot 时返回 dict 不抛);**回归测试覆盖**(`tests/test_interview_agent.py::TestSlotExtractionAlignsWithPolicy` 6 case)— 4 gap × 3 轮对答(communication / process_metric / tech_metric / domain_x)+ 2 fallback 边界(question_plan.slot="" 走 _current_slot / question_plan=None 走 _current_slot);**实测** — `pytest tests/ -q` → 930 + 6 = **936 passed**(R6-D 930 + R6-E Phase 4 6 新增)/ API smoke `.planning/r6e_p4_api_smoke.py` 4 gap 全部 PASS(captured_delta 3 轮 key 顺序为 background → action → result,/draft 200 + 至少 1 bullet);**不修改** `_current_slot` 本体(`_do_rephrase` 还在用)/ `next_question` / `plan_next_question`(R6-B Phase 3 / R6-C.2B 锁定)/ slot 抽取逻辑(R6-C.3 / R6-D 锁定);**不挂 pre-push hook**;**不引入新依赖 / 不引入新 LLM 调用**;baseline 930 → **936 全绿**(6 新增,930 老测试零回退)
  - **R6-F closeout 起:项目回顾 + bug 审核 锁死(2026-07-03 落地)** —— 来自 `.harness/docs/round6-f-project-review-bug-audit-spec.md` + `.harness/docs/round6-f-project-review-bug-audit-report.md`;**核心边界** — 7 phase 全部落地:Phase 0 现场保护 / Phase 1 GitHub 状态 + 文档漂移审核(发现 4 处文档漂移 F-1.1/1.2/1.3/1.4 docs-only patch 修复)/ Phase 2 静态风险扫描(5 项检查 × ~177 处命中,**0 P0 / 0 P1 bug**,5 处 review-needed 全部 owner 决策已落地)/ Phase 3 R6-E Phase 4 同类 bug 深挖(0 新变体)/ Phase 4 默认 rules 路径 + LLM fallback 审核(全部符合 spec 边界)/ Phase 5 Eval report 隐私审核(offline compare 跑通,隐私扫描 0 真实泄漏)/ Phase 6 前端 + API smoke(0 P0 / 1 P2 UI chip 噪音 F-6.1 顺手修 `InterviewAgentPanel.vue:380 :type=""` → `:type="'primary'"`)/ Phase 7 全量验证 + bug triage findings 表;R6-F closeout patch 内容 = docs-only(README / ROADMAP / MEMORY / R6-E spec 4 处 baseline + sync 状态字面替换)+ UI chip fix F-6.1 + cleanup trash(`backend/_r6e_p4_insert_agents_entry.py` 一次性 helper + `.planning/r6f_p6_*.json` 13 个 Phase 6 evidence + `.planning/r6f_p6_api_smoke.py` 临时脚本,保留 `.planning/r6e_p4_api_smoke.py` 与 `.planning/round6-a-ux/` 2 张 PNG 已入库文件);3 个 review-needed 延后 R6-G 单独处理:F-2.1 verifier 失败 sentinel 提示 / F-2.2 llm envelope 重复 except hygiene 清理 / F-2.3 stderr 错误信息脱 `LLM_API_KEY` 字面量(R5-D spec §6.4 严格口子);commit `a3f48b1`;baseline 维持 936(R6-F closeout 不引入新测试);**不挂 pre-push hook**;**决策原则** — 平 rules 保持默认并优先扩 eval set;fallback 高暂停 LLM 投资修 fallback / schema / prompt 边界;GitHub 同步受阻先解认证不堆本地功能
  - **R6-G 起:review-needed 3 项整理 + 测试基线 936→948 锁死(2026-07-03 落地)** —— 来自 `.harness/docs/round6-f-project-review-bug-audit-report.md` §7 延后 3 review-needed 落地;**核心边界** — 改 3 个 backend 文件 + 2 个测试文件:`backend/core/interview_verifier.py`(加 `INTERVIEW_VERIFIER_FAIL_WARNING` sentinel 常量,`verify_draft_card` 在 unsupported/low_confidence>0 时返 sentinel 提示 — F-2.1)+ `backend/core/interview_llm.py`(envelope except hygiene 清理 `except (ValueError, OSError, Exception)` 冗余简化 — F-2.2)+ `scripts/evaluate_interview_agent.py`(stderr 错误信息脱 `LLM_API_KEY` 字面量 / `sk-` 前缀 / `Bearer` 头 — F-2.3)+ `backend/tests/test_interview_verifier.py` 5 case(TestVerifierSentinelF2_1 5: sentinel 常量存在 / 触发条件 1 unsupported / 触发条件 1 low_confidence / 不触发空 sentinel / sentinel 不含 PII)+ `backend/tests/test_interview_eval.py` 7 case(TestEvalStderrSanitizeF2_3 7: stderr 不含 LLM_API_KEY / stderr 不含 sk- / stderr 不含 Bearer / live mode key 错时 stderr 脱敏 / offline mode stderr 路径不变 / 错误信息含脱敏后路径 / 错误信息保留诊断信息)+ `backend/tests/test_interview_llm.py` 0 新 case(F-2.2 行为不变,现有测试覆盖 envelope except 路径);**12 个 R6-G 新 pytest** = TestVerifierSentinelF2_1 5 + TestEvalStderrSanitizeF2_3 7;baseline 936 → **948 全绿**(12 新增, 936 老测试零回退, 117.94s);**不修改** `core/interview_agent.py` 老路径 / `core/interview_prompts.py` / `core/interview_policy.py`(R6-C.2B 路线 B 锁定)/ 任何 frontend;**不挂 pre-push hook**;**不引入新依赖**(纯 stdlib);**不引入新 LLM 调用**;**决策原则** — 平 rules 保持默认(`enable_interview_llm=False`);live eval v2 决策门禁(R6-H)启动条件 = 948 baseline 全过 ✅ + 用户已配 LLM key + 前端可跑 10+ 轮真实对话;commit `ae0e89b`
  - **R6-H 起:docs-only 整理 + 安全升级 锁死(2026-07-07 落地)** —— 来自 `.harness/docs/round6-h-live-eval-v2-decision-gate-spec.md` §10 路径 A 落地 + 全维度体检后的 docs/hygiene/sec 收尾;**核心边界** — **5 个 commit 全部 docs / hygiene / 依赖升级,0 新业务代码改动,0 新测试**;改动范围:(1) `ba67743` docs(round6-h): `.harness/docs/architecture.md` 补系统架构索引段(指向 `system-architecture.md`)+ `.harness/docs/round6-h-live-eval-v2-decision-gate-spec.md` 补 §10 路径 A 拍板记录(commit 列表 / 测试基线 / 决策门槛)+ `scripts/verify.ps1` 同步 948 baseline 字面(共 3 文件 +93/-3 行);(2) `a50635e` docs(round6-h): 新增 `.harness/docs/system-architecture.md` 332 行 5 维度系统架构总览(前端 / 后端 / 数据 / agent / 脚本);(3) `2c28376` chore(round6-h): `.gitignore` 加 2 项 ignore(`.codebuddy/` 临时 IDE 配置 + `架构设计文档.md` 与 system-architecture.md 重复副本);(4) `cadacef` docs(round6-h): `README.md:16` 修过期字面("ahead 3 commits" → "已与 `origin/main` 同步 (HEAD = `bd20591`)" — **本轮提交时点的 HEAD**);(5) `0556c91` chore(round6-h-sec): `backend/requirements.txt` 安全升级 — `fastapi` 升 0.128.0 → **0.134.0**(0.128.0 隐式锁 `starlette<1.0.0` 与 CVE-2026-48710 冲突,0.134.0 起 starlette 约束放开为 `>=0.46.0` 无上限)+ 显式锁 `starlette>=1.0.1`(CVE-2026-48710 BadHost Critical 要求)+ `python-multipart` 0.0.20 → **0.0.32**(覆盖 CVE-2026-24486 路径遍历 + CVE-2026-53539/42561/40347/51493/51497 等 6 个 DoS,0.0.18 修复路径遍历但 0.0.32 才补全 DoS);**实测** — `D:\python3.11\python.exe -m pytest tests/ -q` → **948 passed in 16.34s**(R6-G 117.94s 差异 = 测试并行度差异,不是测试集变);`starlette==1.3.1` + `python-multipart==0.0.32` 实测确认;前端 `npx vue-tsc --noEmit` 0 error + `npm run build` 成功;本地 main 与 origin/main 已同步(0 ahead / 0 behind,HEAD = `0556c91`);**不修改** 任何 `backend/core/` / `backend/api/` / `scripts/` / `frontend/src/` 业务文件(**`requirements.txt` 是依赖清单不算业务改动**);**不挂 pre-push hook**;**不引入新依赖**(纯 stdlib + 已有依赖版本号调整);**不引入新测试**(行为不变机械依赖升级);**不引入新 LLM 调用**;**R6-H 之前误判澄清** — 体检报告初版标 "git history PII 残留" 为 P1,核实后发现 `b29334cb` scrub commit 已脱敏,blob 里只剩 `13800000000` / `your_email@example.com` 占位符(用 `git show b29334cb --no-renames | grep -E "1[3-9]\d{9}|email"` 验证 0 处真实 PII);**决策原则** — 安全升级属于"卫生",不阻塞业务(本地单用户 blast radius=0,AGENTS.md 明示不暴露公网);live eval v2 仍 deferred(等用户在 chat panel 跑完 10+ 轮真实对话后启动 Phase 2);R6-H §10 路径 A 已落档但**不**作为下一轮自动启动 — 由用户决定是否跑真实 LLM key 接入 + Phase 4 真实对话 → Phase 5 live eval v2 报告;commit `0556c91`(HEAD)
  - **R6-J 起:eval set 扩到 20 条 boundary 锁死(2026-07-14 落地)** —— 决策档位 "未通过 (LLM 持平, offline 限制)" 跟 R6-H 同档;**核心边界** — 只改 1 个 scripts 文件 + 1 个测试文件:`scripts/evaluate_interview_agent.py` 加 10 条新 boundary sample(`EVAL_SET_BOUNDARY` 块 5 类场景 — chaos 2 / multi_slot 2 / long_context 2 / jargon 2 / process_metric_boost 2 — 验证 LLM 优势场景)+ `EVAL_SET_ALL` = `EVAL_SET_PLAN_BASELINE + EVAL_SET_SIMULATED_USER_V1 + EVAL_SET_BOUNDARY` 三组合并,`write_report` / `main()` 走 20 条全跑;每条 sample 加 `product_goal` (枚举 `three_turn_friendly` / `full_fact_coverage`) + `contract_note` 字段(R6-C.2A contract 兼容, 每条 contract_note 开头加 "完整项目事实覆盖目标" 字面量);**11 个 R6-J pytest** = `tests/test_interview_eval.py` TestR6JBoundarySamplesLoading 4 (boundary sample 加载 / 5 类场景存在 / ID 不重复 / contract 字段完整) + TestR6JProcessMetricCoverage 2 (process_metric 覆盖 ≥2 + boost keyword 命中) + TestR6JBoundaryContractNote 2 (contract_note 字面量 / product_goal 枚举) + TestR6JBoundaryStructure 3 (每个 expected_slots 非空 / 20 条总 / field schema 稳定);baseline 948 → **959 全绿**(11 新增, 948 老测试零回退);`scripts/evaluate_interview_agent.py --mode offline --extractor compare` 实测 `total=20 rules schema_pass=0.30 llm schema_pass=0.30 llm fallback_rate=1.00`(跟 R6-C.2A baseline 一致, offline 模式 LLM 不发网络);`scripts/evaluate_interview_agent.py --mode live --extractor compare` 实测 12.5 min 跑完 40 sample(offline 模式 20 + live llm 模式 20),fallback=0%, fabrication=0, avg_latency_ms=32760;**不挂 pre-push hook**;**不修改** `core/` / `interview_llm.py` retry/schema/token / `core/interview_policy.py` / `core/interview_verifier.py` / 任何 frontend;**决策档位:未通过 (LLM 持平, offline 限制)** — 走方案 C 不动合同, offline 模式无法证伪 LLM 收益;commit `c32f566` / `c8b7f5e` / `c82ab92`
  - **R6-H live v2 partial closeout 起:LLM 端点稳定性 ground truth 锁死(2026-07-14 落地)** —— 来自 `.harness/docs/round6-h-live-eval-v2-llm-stability.md`;**新 ground truth** — user 跑 `--mode live --extractor compare` 真实 LLM 调用场景下,LLM 端点稳定性不可控(24 min 第一次响应 + 11 min 正常 + 32 min 端点卡 0 trace 增长 = 56 min 不可用窗口,user Ctrl+C 中断),`urllib.request.urlopen(timeout=60)` 在 Windows + 长 hang 场景下不严格抛 TimeoutError,导致 R6-K 计划的 circuit breaker `record_hang` 触发不到(urllib 协议层已知问题);**决策档位:未通过 (LLM 服务稳定性)** (R6-H §5.3 决策表新增档位, 32 min LLM 端点卡死是真实生产风险);**不挂 pre-push hook**;**不引入新依赖**;**决策原则** — LLM 端点稳定性属于基础设施层,不是业务逻辑可解决;`commit fb334af` / `625b873` (R6-H live v2 partial closeout 2 commit)
  - **R6-K 起:LLM 端点 circuit breaker + 前端降级 UI 锁死(2026-07-14 落地)** —— 来自 `.harness/docs/round6-k-llm-resilience-spec.md`;**核心边界** — 实施范围只 1 (circuit breaker) + 2 (降级 UI),**不做 3 (retry backoff) / 4 (端点挂 disabled toggle)**, 留 R6-K+ 后续;**改 5 个 backend 文件 + 2 个 frontend 文件 + 1 个 scripts/conftest**:`backend/core/llm_circuit_breaker.py` (新建, 128 行) `CIRCUIT_CLOSED/OPEN/HALF_OPEN` 常量 + `CircuitBreaker` class + `get_circuit()` / `reset_circuit()` helper + 状态机 closed→open→half_open (60s open + 60s probe) + hang 立即 open 不等累计(32 min 端点卡场景的关键);`backend/core/interview_llm.py` 顶部加 `from core.llm_circuit_breaker import get_circuit` + `_call_llm_for_slot_extraction` 顶部 circuit check + except 拆 4 个分支(record_success / record_failure / record_hang, 不动 retry/schema/token 路径);`backend/core/interview_agent.py` `_do_answer` 加双层保险(Layer 1 短路 return None + Layer 2 强制 mode="rules" + mode_warning);`backend/api/interview.py` `StartResponse`/`ReplyResponse` 加 `circuit_state` (closed/open/half_open) + `circuit_remaining_seconds` 字段, 端点透传 `get_circuit().snapshot()`;`backend/tests/conftest.py` autouse fixture `_r6k_reset_circuit_breaker` 跨 module 隔离;`backend/tests/test_llm_circuit_breaker.py` 10 case + `backend/tests/test_interview_api.py::TestCircuitBreakerApiResponse` 4 case = **14 个 R6-K pytest**, baseline 959 → **973 全绿**(14 新增, 959 老测试零回退);`frontend/src/api/index.ts` `InterviewStartResponse`/`InterviewReplyResponse` 加 2 字段(联合类型)+ `frontend/src/components/InterviewAgentPanel.vue` 加 `circuitState` ref + `circuitTagInfo` computed + `syncCircuitState` helper + `start/stopCircuitCountdown` setInterval + chip 渲染(open 紫红 danger / half_open 黄 warning / closed 不显示)+ 倒计时(open 状态时每秒 -1)+ `isLlmToggleHidden` 控制 enable_interview_llm toggle 显示 + `onUnmounted` cleanup;**实测** — `vue-tsc --noEmit` 0 error + `npm run build` 成功 (5.36s);**不挂 pre-push hook**;**不引入新 LLM 调用**;**不引入新依赖** (纯 stdlib + TYPE_CHECKING);**不修改** `core/interview_prompts.py` / `core/interview_policy.py` / `core/interview_verifier.py` / `core/llm_rewriter.py` / `PROMPT_VERSIONS` / `INTERVIEW_POLICY_GAP_CRITICAL_SLOTS` / `INTERVIEW_LLM_NO_KEY_WARNING` / `INTERVIEW_VERIFIER_FAIL_WARNING` sentinel / default `enable_interview_llm=False`;**决策档位:暂缓 LLM 投资, 保持 rules 默认** (强化 R6-J §7.3 (d), 新增 LLM 端点稳定性 ground truth);**R6-K 部署保留**给生产 chat panel 加保护层(虽然测试场景没真触发, 32 min 端点卡时 R6-K 仍能切回 rules 不卡用户);commit `0352758` / `4b755ce` / `05459ee` (R6-K 主实施 3 commit)
  - **R6-K live eval v2 verification 起:决策档位维持锁死(2026-07-14 落地)** —— 来自 `.harness/docs/round6-k-live-eval-v2-verification.md`;**核心边界** — user 跑 `--mode live --extractor compare` 验证 R6-K 部署, 12.5 min 跑完 40 sample (20 rules + 20 llm), LLM 真实调用 0% fallback, fabrication=0, avg_latency_ms=32760 (~32s, 跟 R6-H live v1 一致);**新 ground truth** — R6-H live v2 partial 撞 32 min 可能是 MiniMax 偶发,不是常态;**决策档位维持 "未通过 (LLM 持平)"** - 跟 R6-H / R6-J 一致;user 一度误判"trace 8 min 0 增长 = 卡死", 实际是 eval 跑完 19:30:51 写完报告后正常 exit;**circuit breaker 失效场景 (R6-K v2 实测)** — `urllib.request.urlopen(timeout=60)` 在 Windows + 长 hang 场景下不严格抛 TimeoutError, 32 min 端点卡时 record_hang 触发不到 — 代码层修复没问题, urllib 协议层已知问题;**不挂 pre-push hook**;**不引入新依赖**;**0 新 pytest** — 纯验证报告;**不修改** `core/` / `scripts/` / `frontend/src/`;commit `e8eeb25` / `175a37b` (R6-K verification 2 commit)
  - **R6-K Postman collection + 字段一致性检查工具 锁死(2026-07-14 落地)** —— 来自 `.harness/docs/round6-k-postman-closeout.md`;**核心边界** — (1) `tests/postman/R6-K_Interivew_Agent.postman_collection.json` (新建, 315 行, Postman 2.1 格式, 7 步流程 + 14 Tests 断言);(2) `scripts/check_postman_schema.py` (新建, 220 行, 字段名静态检查工具) — AST 解析 `backend/api/interview.py` Pydantic Response 字段 + `backend/main.py` HealthResponse hardcode + `core/interview_agent.py` save_card 字面量嵌套字段 + Postman collection `pm.expect(json.xxx).to` 字段路径 → 对账,字段名 typo / schema 改名 catch,业务正确性(1st turn 抽哪个 slot)不 catch;**3 个跑法** — `D:\python3.11\python.exe scripts/check_postman_schema.py`(静态, 16 assertions 0 failures) + `pytest tests/test_check_postman_schema.py -q` (16 passed) + `pytest tests/ -q` (973 baseline + 16 新增 = **989 passed** 0 回归);**16 个 pytest 锁** = TestModuleLoading 1 + TestParsePostmanAssertions 2 + TestParsePydanticResponses 2 + TestParseSaveCardLiteral 1 + TestEndpointMapping 2 + TestSchemaPathCheck 4 + TestMainEndToEnd 3 (good / bad project_id / bad captured_dleta) + TestRealCollection 1;**2 个原 bug 修复** — step 3 `captured_delta.responsibility` (1st turn policy 实际抽 background 不是 responsibility) + step 7 `material_ref.project_id` (字段名是 `id` 不是 `project_id`,手抄时写错);**Postman Runner 实测** — 26/26 全 PASS(用户 2026-07-14 20:54 跑通);**反向测试** — 故意改成 project_id 后工具精确 catch(`field 'material_ref.project_id' not in nested schema for 'material_ref'`),exit 1;**不挂 pre-push hook**;**不引入新依赖** (纯 stdlib `re` + `ast` + `json` + `pathlib` + `importlib`);**不修改** `core/` / `backend/api/` / `frontend/src/` 业务文件;**工具能力圈**: ✓ 字段名 typo / schema 改名 / 嵌套层级错 / 字段名拼写; ✗ 业务正确性(1st turn 抽哪个 slot / expected 哪个) / 时序 / policy 行为 — 这些需真跑 server 验证;**后续 round 候选** — Newman CLI 接 pre-push (npm install) / FastAPI OpenAPI → Postman 自动生成 / check_postman_schema 接 scripts/verify.ps1 / chat panel 真实 10+ 轮验证 R6-K 生产场景 / retry 指数 backoff;commit `00ae55c` / `9df5294` (R6-K Postman collection 2 commit)

  - **R6-B Phase 5 起:eval compare 锁死(2026-06-30 落地)** —— 来自 `.harness/docs/round6-b-interview-agent-intelligence-spec.md` §8 实施;**核心边界** — `scripts/evaluate_interview_agent.py` 加 `--extractor {rules, llm, compare}` CLI 参数(默认 `rules` 字节级一致 R6-A Phase 5 行为),复用 R5-D `evaluate_agent_workflow.py` 的 `_resolve_eval_mode` / `_get_llm_eval_config` / `_check_pii_safe` / `_percentile` 4 个 helper(spec §8 + 既有规范, **不**重写),**不修改** `evaluate_agent_workflow.py` / `evaluate_prompt_versions.py`;5 类 fallback_category 常量(对齐 R5-C Phase 1):`none` / `llm_disabled_fallback` / `tool_error_fallback` / `schema_retry_fallback` / `workflow_abort_fallback`;`_evaluate_one(sample, materials, *, extractor_mode=EXTRACTOR_RULES)` 新增 keyword-only `extractor_mode` 参数,内部 `enable_llm_intent = (extractor_mode == EXTRACTOR_LLM)` → `create_session(..., enable_interview_llm=enable_llm_intent)`,session 实际 `interview_mode` 由 `core.interview_agent._decide_interview_mode` 决定;`_classify_interview_fallback_category(extractor_mode, actual_mode, error_type)` 4 类分类:error 优先 / rules+rules → none / llm+llm_assisted → none / llm+rules → llm_disabled_fallback;**EvalRow 新增 4 字段** — `extractor_mode` / `fallback_category` / `low_confidence_slot_count` / `total_slot_meta_count`(后两个从 `session.slot_meta` 聚合 confidence<0.6 条数,沿用 `INTERVIEW_LOW_CONFIDENCE_THRESHOLD=0.6` 阈值,bool confidence 拒绝);**compute_metrics 新字段** — `low_confidence_slot_rate`(low/total)/ `by_extractor` 二次分组(compare 模式用)/ `fallback_category_breakdown` 5 类分布;**write_report 新章节** — `## 二、{extractor_label} 路径基线`(标题按 extractor 模式变)/ `## 2.5、Rules vs LLM-assisted 对照` (compare 模式专属,8 指标 + Delta 块)/ `## 三、fallback_category 分布` (5 类展示)/ 每条样本摘要新加 `extractor` + `fb_cat` + `low_conf=N/M` 字段;**offline 模式不发网络硬保证** — main() 决策表: rules 永远走规则版 / llm+offline 走规则版 + llm_disabled_fallback / llm+live+无 key → RuntimeError(错误信息**不含** key 值 / env var 名,沿用 R5-D Phase 1 `TestEvalModeNoKeyLeak` 模式)/ llm+live+有 key 走真 LLM 路径(`urllib POST /chat/completions`,沿用 `core.interview_agent._call_llm_for_slot_extraction`);**隐私边界** (spec §8 + §12 + AGENTS.md)— report / stdout **绝不**含 user_message / prompt / raw response / source_span 明文 / draft_bullets 完整原文 / API key 值 / env var 名(LLM_API_KEY) / key 前缀(sk-) / Bearer 头 — 报告边界说明用 "API key 类凭据" / "LLM 抽取源 span 明文" 通用描述,避免 spec §14 `Select-String -Pattern "用户原文哨兵|LLM_API_KEY|BEGIN PROMPT|source_span"` 假阳性;**不挂 pre-push hook** (spec §12 #3 默认手动脚本);**不引入新依赖** (纯 stdlib + dataclass + 已有 helper);**不修改** `core/interview_agent.py` / `core/llm_rewriter.py` / `scripts/evaluate_agent_workflow.py` / `scripts/evaluate_prompt_versions.py` / 任何 `core/` 模块;**老路径字节级一致** — `--extractor rules`(默认)/ `--mode offline` 时 `extractor_mode='rules'` / `fallback_category='none'` / `fallback_used=False` / 报告标题含 "规则版" 标签(840 老测试零回退);**23 个 R6-B Phase 5 新 pytest** = `tests/test_interview_eval.py` (TestPhase5Helpers 4 — count_low_confidence_slots 返回 (low, total) / None/空 dict 兜底 (0, 0) / bool confidence 拒绝不计入 low / _classify_fallback_category 4 类优先级 + error 优先 / TestPhase5ExtractorModes 5 — rules 模式默认行为字节级一致(extractor_mode=rules / fallback_category=none / 报告标题含"规则版")+ llm 模式 offline 全标 llm_disabled_fallback(fallback_used=True / fb_cat=llm_disabled_fallback / 报告含 llm_disabled_fallback 标记)+ compare 模式跑 2N 行(rules 10 + llm 10)/ 双组 fallback 状态相反 / 报告含 "Rules vs LLM-assisted" 章节 + Delta 块 + compare offline 不调 urlopen (mock + assert_not_called) + 非法 extractor_mode 兜底 rules (fallback_used=False) / TestPhase5Metrics 5 — metrics 含 low_confidence_slot_rate (0-1 float) / p95_latency_ms (非负 int) / fallback_category_breakdown 是 5 类 dict (key 集对齐 R5-C) / by_extractor 分组 rules + llm 互不混入 + 各 total=N 总和=全局 / 空 rows 返完整 schema 含 0 值 + 5 类 fallback 仍在 / TestPhase5Report 6 — compare 报告 "Rules vs LLM-assisted" 含 8 指标 + Delta 块 + compare 报告 "fallback_category 分布" 含 5 类 / 三种 extractor 模式报告都含 low_confidence_slot_rate 字段 / compare 报告不含 user_message 原文(10 抽样短语) / llm offline 报告行级 fb_cat 标 N 次 (N=样本数) / 报告不含 prompt sentinel + source_span sentinel / TestPhase5Cli 3 — main() 跑 --extractor compare 写报告含对照表 + exit 0 / main() 跑 --extractor rules 标题含 "规则版" 不含 "Rules vs LLM-assisted"(compare 专属) / 非法 extractor_mode 走 argparse SystemExit code≠0);baseline 840 → **863 pytest 全绿** (23 新增, 840 老测试零回退);**实测耗时** — `pytest tests/test_interview_eval.py` 29 passed / 0.81s;`pytest tests/` 863 passed / 62.05s;离线 compare 实测输出 `total=20 (rules=10 + llm 意图=10) rules schema_pass=0.30 llm schema_pass=0.30 llm fallback_rate=1.00` 符合预期;**R6-B 后续 Phase 顺序** — Phase 1 slot_meta provenance ✅ → Phase 2 API mode 开关 ✅ → Phase 3 confidence-aware policy ✅ → Phase 4 draft verifier ✅ → Phase 5 eval compare ✅ → Phase 6 frontend 最小呈现(spec §13 非目标:自由聊天助手 / 模拟面试训练 / 自动投递 / live eval 报告自动入库等)

  - **R6-B Phase 6 起:frontend 最小呈现 锁死(2026-06-30 落地)** —— 来自 `.harness/docs/round6-b-interview-agent-intelligence-spec.md` §9 实施;**核心边界** — frontend **不**显示 Agent Workflow / trace / schema / ToolResult(spec §9 明示禁止);**不**展示 LLM prompt 正文 / raw response / source_span 明文;**不**修改任何 backend / `scripts/` / `core/` 模块 — 后端 Phase 0+1+2+3+4+5 字节级一致;**frontend 改动 3 个文件** + 类型加 6 个:`frontend/src/api/index.ts` 加 `InterviewMode` / `ExtractionMode` 类型 + `ExtractionSummary`(4 字段)/ `QuestionPlan`(3 字段)/ `VerificationSummary`(5 字段) 3 个 interface + `InterviewStartRequest.enable_interview_llm?: boolean` 字段 + `InterviewStartResponse.interview_mode?: InterviewMode` + `mode_warning?: string | null` + `InterviewReplyResponse.extraction_summary?: ExtractionSummary | null` + `question_plan?: QuestionPlan | null` + `InterviewDraftCard.verification?: VerificationSummary` + `confidence_notes?: string[]`(全部 optional,老后端不返也不报错,字节级一致锁死);`frontend/src/components/InterviewAgentPanel.vue` 加 `enableInterviewLlm: ref(false)` (EMPTY 状态切换,默认关闭)+ `interviewMode: ref<InterviewMode>('rules')` + `modeWarning: ref<string | null>(null)` 3 个 state + `modeTagInfo` computed 三态 header 标签(已回退规则模式 warning / 智能抽取 success / 规则模式 info)+ `onStart` 透传 `enable_interview_llm: enableInterviewLlm.value` + 从响应读 `res.interview_mode` / `res.mode_warning` + 丢弃 / 父层变更时同时重置 mode 标签;`frontend/src/components/InterviewDraftCard.vue` 加 `confidenceNotes` / `verification` / `needsSaveConfirm` 3 computed(spec §7+§9)+ 在 warnings 区域后插入置信度提示 + 事实核验摘要 2 个面板(紫边框区分,counts 显示,不含 draft_bullets 原文)+ `onSave` 在 emit 前对 `unsupported_claims > 0 || low_confidence_claims > 0` 触发 `ElMessageBox.confirm`(计数 + 提示文案,**不**展示 bullet 原文);**布局防御** — toggle 行用普通 `div + flex` + `el-tooltip`,**不**用 el-popover / drawer 嵌套,移动端 drawer 内 `ip-toggle-row` 高度 ~32px 不挤压 EMPTY 卡片;confidence_notes / verification 区域用普通 `div + 浅紫边框`,**不**产生 portal / overlay,移动端可随 drawer 滚动;**R6-A Phase 3 隔离边界** — `InterviewAgentPanel.vue` **不** import `AgentSummary` / `EvidenceSummary` / `ExternalResumePerspective` / `BulletEvaluation` 等 R5 workflow 诊断字段(spec §3.3),**只**消费自己新增的 `InterviewXxx` + `InterviewMode` / `ExtractionSummary` / `QuestionPlan` / `VerificationSummary` 4 类型;**R6-B 老后端字节级一致** — 后端 `enable_interview_llm=False`(默认)时 `interview_mode='rules'` / `mode_warning=None`,前端不切换 toggle 时 `/draft` 仍照常返 `verification + confidence_notes`(Phase 4 行为不变);**0 新 pytest** —— 前端无 Vitest 配置,沿用 R6-A Phase 3 + R6-B §10 Phase 6 验收方式:`vue-tsc --noEmit 0 error` + `npm run build` 成功 + 桌面 1280x800 + 移动 375x812 浏览器 smoke;**实测** — `cd frontend && npx vue-tsc --noEmit` 0 error + `npm run build` 成功(`dist/index.html 0.48kB / css 369kB / js 1104kB`)+ 后端基线维持 863 passed(R6-B Phase 6 不引入新后端逻辑);**不挂 pre-push hook**;**不引入新依赖**;**不修改** `core/` / `scripts/` / 后端任何文件;**R6-B 末段边界** — Phase 6 是 R6-B 最后一 Phase(spec §11 落地顺序建议):Phase 1+2+5 先落地(评估收益决策点),Phase 3+4+6 再落地(产品化闭环);Phase 6 落地后 R6-B spec §11 §12 全部满足(spec §8 product gate: live 收益证据由用户跑 10+ 轮真实对话后跑 live eval 报告,本 round **不**自动入库 live 报告)
  - **R6-B Phase 2 起：API mode 开关 锁死** —— 来自 `.harness/docs/round6-b-interview-agent-intelligence-spec.md` §5.3+§2.2 实施;`StartRequest` 新增 `enable_interview_llm: bool = False` keyword-only 字段(老路径字节级一致 — 不传时等价于 R6-A Phase 1);`StartResponse` 新增 `interview_mode: str = "rules"` + `mode_warning: str | None = None` 字段(都有默认值,老前端不消费也不报错);`ReplyRequest` **不**新增 `enable_interview_llm`,reply 沿用 `session.interview_mode`(多传字段被 Pydantic 忽略,不注入 session);`ReplyResponse` 新增 `extraction_summary: dict | None` + `question_plan: dict | None` 字段;`create_session(target_role, jd_text, materials, *, enable_interview_llm=False)` keyword-only 入参,默认 False 字节级一致;`_decide_interview_mode(enable_interview_llm) -> (mode, warning)` 决策表: enable=False → ("rules", None) / enable=True + LLM_API_KEY 在 env → ("llm_assisted", None) / enable=True + 无 key → ("rules", "智能抽取不可用, 已使用规则模式");`_do_answer` 内部用 `use_llm = (session.interview_mode == "llm_assisted")` 决定 `extract_slots(..., llm_enabled=use_llm)`(老路径 rules session 仍 `llm_enabled=False` 字节级一致);`_build_extraction_summary(new_meta_entries, captured_delta, intended_mode)` 构造 spec §5.3 schema 4 字段 `extractor: rules|llm|mixed` / `fallback_used: bool`(intended_mode=llm_assisted 但 extractor != "llm" 即 fallback)/ `captured_slots: list[str]`(从 captured_delta 顶层 key 抽,排除 _ 前缀) / `low_confidence_slots: list[str]`(< 0.6 阈值常量 `INTERVIEW_LOW_CONFIDENCE_THRESHOLD`);`_build_question_plan(session)` Phase 2 占位实现返 `{slot, reason_code: "phase2_placeholder", low_confidence_slots}`(从 `session.slot_meta` 聚合 confidence<0.6 的 slot 名);**隐私边界** (spec §5.3 + AGENTS.md) — `StartResponse` / `ReplyResponse` / `extraction_summary` / `question_plan` / `mode_warning` 4 层链路**绝不**含 API key 值 / env var 名(`LLM_API_KEY`)/ key 前缀(`sk-`)/ `Bearer` 字样 / LLM prompt 正文 / source_span 明文 / user_message 明文 / confidence 数字;`extraction_summary` schema 限定只 4 字段(无 source_span / user_message / confidence / source_span_hash);**R6-B Phase 1 R5-E 边界** 保持 — `backend/core/interview_agent.py` 文件任意位置不出现 `from core.llm_rewriter import` / `import core.llm_rewriter`(Phase 4 锁死的 6 个禁列模块仍生效);**不挂 pre-push hook**;**不引入新依赖**(纯 stdlib);**不修改** `core.llm_rewriter.py` / `core.agent_workflow.py` / `core.agent_tools.py` / `core.tool_schema.py` / `core.interview_prompts.py` / `core.jd_parser.py` / `core.generator.py`;**老路径不变** — `enable_interview_llm=False`(默认)时 `session.interview_mode="rules"` / `mode_warning=None` / `_do_answer` 仍 `llm_enabled=False` 走 `_extract_slots_by_rules` / StartResponse 字段都从 dataclass 默认值拿 / `interview_mode="rules"` 字段值不变,739 老测试零回退;**11 个 R6-B Phase 2 新 pytest** = `tests/test_interview_api.py`(TestStartModeSwitch 4: default-rules-no-warning / explicit-False-rules-no-warning / enable-True-no-key-rules-with-warning / enable-True-with-key-llm_assisted + TestReplyUsesSessionMode 3: rules-session-extraction-extractor-rules / llm_assisted-session-mock-LLM-extractor-llm / reply-multi-passes-enable_interview_llm-ignored + TestResponsePrivacy 4: start-no-key-leak / start-no-prompt-leak(用 SLOT_EXTRACTION_SYSTEM_PROMPT[:50] 子串断言)/ reply-no-source_span-no-user_message-no-key-leak + reply-no-key-no-Bearer-no-env-var-name-leak);**R6-B 后续 Phase 顺序** — Phase 1 slot_meta provenance ✅ → Phase 2 API mode 开关 ✅ → Phase 3 confidence-aware policy → Phase 4 draft verifier → Phase 5 eval compare → Phase 6 frontend 最小呈现;**R6-B 不**做 — 自由聊天助手 / 模拟面试训练 / 自动投递 / 账号云端多用户 / 向量数据库 Redis 后台队列 / 完整对话持久化 / LLM 自动保存素材 / 重构 LLM slot extraction 到新模块 / 改默认 resume rewrite prompt / rollout R5-E prompt winner / live eval report 自动入库(spec §13 非目标清单)
- **R6-B Phase 3 起:confidence-aware policy 锁死(2026-06-30 落地)** —— 来自 `.harness/docs/round6-b-interview-agent-intelligence-spec.md` §6 实施;**核心边界** — `backend/core/interview_policy.py` 是 **deterministic 纯函数**, 不调网络, 不调 LLM, 不读 env var, 不 mutate session(测试用 snapshot diff 锁);`plan_next_question(session) -> dict` 8 步优先级链(spec §6): 0. selected_gap=None / suggested_slots=空 → no_more / no_gap_selected; 1. skip_count >= MAX_CONSECUTIVE_SKIPS → force_draft / force_draft_skip_limit(can_draft=True); 2. turn_count >= MAX_TURNS_PER_GAP → force_draft / force_draft_turn_limit(can_draft=True); 3. 任一 CAN_DRAFT_CONDITIONS 未满 → ask 缺的那个, reason=missing_required_before_draft(选"差得最少"的 combo, first combo = (background, action, result) 优先); 4. captured 但 confidence < 0.6 → ask 该 slot recheck, reason=low_confidence_recheck; 5. turn_count >= MAX-1 + 仍缺 metric (combo1 满时) → ask metric, reason=near_limit_priority_result_metric; 6. 按 gap.suggested_slots 顺序 → ask 下一个未 captured, reason=next_suggested_slot; 7. **anti-repeat** — last_asked_slot == next_slot + last_conf >= 0.4 → 切去 suggested 里另一个未 captured, reason=anti_repeat_switch_slot(last_conf < 0.4 时不切,允许再问); 8. 所有 suggested captured → no_more / all_gap_slots_covered(**can_draft 用 _already_can_draft(session) 算**,避免 step 8 误报 can_draft=False 让前端以为不能 draft);**返回 schema 6 字段** — `slot` / `reason_code` / `kind: ask_slot|force_draft|no_more` / `next_question_kind` / `can_draft` / `low_confidence_slots`;**constants** — `INTERVIEW_POLICY_LOW_CONFIDENCE = 0.6`(跟 interview_agent 对齐)/ `INTERVIEW_POLICY_ANTI_REPEAT_CONFIDENCE = 0.4` / 9 个 `INTERVIEW_POLICY_REASON_*` 常量 + 3 个 `INTERVIEW_POLICY_KIND_*` 互斥枚举;**`interview_agent.py` 接入改动** — `next_question()` 委托 `plan_next_question(session)` 选 slot, 写 `session.message_log.append({"kind": "asked", "slot": slot, "turn": turn_count})(供 anti-repeat 读 last_asked_slot)+ session.question_plan = {slot, reason_code, low_confidence_slots, kind, can_draft}(**force_draft 路径也写**,便于前端审计 reason_code);`_build_question_plan(session)` 替换 Phase 2 placeholder, 转调 plan_next_question 返 spec §5.3 schema 3 字段 `{slot, reason_code, low_confidence_slots}`;`_do_rephrase(session)` **不调 next_question**(避免 policy 跑 anti-repeat 换 slot,违反 spec §6 防重复), 直接读 `session.question_plan.slot` 渲染 text 加 `[换个问法]` 前缀;`_do_switch_gap(session)` **清空 message_log / slot_meta / question_plan**(避免跨 gap 混用, Phase 1 已有清空 captured_slots/skip_count/turn_count/draft_card, Phase 3 新增清 message_log);**隐私边界** (spec §6 + §12 + AGENTS.md) — policy module AST 静态扫描 import 不含 urllib/requests/httpx/openai/anthropic/llm_rewriter, 不读 os.environ / os.getenv, 不引用 LLM_API_KEY 字面量(测试 `TestPlanPrivacyGuarantee::test_policy_no_*_via_ast` 锁);返回 dict 不含 user_message / source_span / draft_card / API key / jd_text;**R5-E 边界保持** — interview_agent.py 顶部 import `from core.interview_policy import (INTERVIEW_POLICY_KIND_ASK, INTERVIEW_POLICY_KIND_NO_MORE)`(不 import plan_next_question 函数本身,函数内 lazy import 防循环);**不挂 pre-push hook**;**不引入新依赖**(纯 stdlib + dataclass);**不修改** `core.llm_rewriter.py` / `core.agent_workflow.py` / `core.agent_tools.py` / `core.tool_schema.py` / `core.interview_prompts.py` / `core.jd_parser.py` / `core.generator.py`;**老路径不变** — `enable_interview_llm=False` 字节级一致, `apply_action(ANSWER)` 流程 768 老测试零回退;**41 个 R6-B Phase 3 新 pytest**(实测后 = 30 + 12 - 1 合并冗余 = 41) = `tests/test_interview_policy.py`(新增 29 case: TestPlanPriorityChain 12 — step0_no_gap + step0_empty_suggested + step1_skip_forces_draft(测 captured={} + captured={"background":"X"} 两种,合并原 priority_chain_skips_steps)+ step2_turn_forces_draft + step3_missing_required + step4_low_confidence + step5_near_limit_metric + step5_does_not_fire_when_step3_still_missing(测 step3 优先于 step5)+ step6_next_suggested + step7_anti_repeat_switch + step7_anti_repeat_low_conf + step8_all_covered / TestPlanOutputSchema 3 — shape + can_draft_true_when_combo_satisfied + low_confidence_slots_sorted_unique / TestPlanPrivacyGuarantee 5 — no_user_message + no_source_span + no_network_imports_via_ast + no_llm_rewriter_import_via_ast + no_llm_api_key_read_via_ast / TestPlanPurity 3 — does_not_mutate_session + is_deterministic + does_not_raise_on_malformed / TestPlanConstants 4 — low_confidence_threshold_is_0_6 + anti_repeat_threshold_is_0_4 + reason_codes_are_unique + kind_constants_unique / TestPlanApplyActionIntegration 2 — after_answer_advances_missing_chain + after_full_combo_can_draft)+ `tests/test_interview_agent.py`(新增 TestPhase3PolicyIntegration 12 case: next_question_uses_policy / no_gap_returns_empty / writes_message_log_for_anti_repeat / writes_question_plan_for_audit / skip_limit_returns_empty / turn_limit_returns_empty / rephrase_does_not_change_slot / rephrase_stays_on_current_slot_even_if_policy_would_switch(防 anti-repeat 误切) / rephrase_does_not_write_extra_message_log_entry / switch_gap_clears_message_log_slot_meta_question_plan / switch_gap_low_confidence_does_not_leak / question_plan_response_field_after_reply);baseline 768 → **809 pytest 全绿**(41 新增, 768 老测试零回退);**Phase 2 占位回退** — `_build_question_plan` 不再返 `reason_code="phase2_placeholder"`,改为 policy 真实 reason_code;`tests/test_interview_api.py::TestReplyUsesSessionMode::test_reply_in_rules_session_uses_rules_extractor` 同步更新断言(改为 `qp["reason_code"] != "phase2_placeholder"` + 在 INTERVIEW_POLICY_REASON_{MISSING_REQUIRED, NEXT_SLOT, NO_MORE} 集合中);**真 bug 修复** — `_make_empty_plan(NO_MORE)` 之前 hardcode `can_draft=False`,导致 step 8 "所有 suggested captured" 时前端误以为不能 draft;改为传 `session=session` 参数, 计算 `_already_can_draft(session)`(测试 `test_step8_all_gap_slots_covered_returns_no_more` 锁 can_draft=True);

  - **R6-B Phase 4 起:draft verifier 锁死(2026-06-30 落地)** —— 来自 `.harness/docs/round6-b-interview-agent-intelligence-spec.md` §7 实施;**核心边界** — `backend/core/interview_verifier.py` 是 **deterministic 纯函数** 模块 (re + dict), 不调网络, 不调 LLM, 不读 env var, 不 mutate session (沿用 Phase 3 policy 边界, 测试用 snapshot diff 锁);AST 静态扫描 import 不含 urllib/requests/httpx/openai/anthropic/llm_rewriter/agent_workflow/agent_tools/tool_schema (测试 `TestVerifierNoNetwork::test_verifier_module_no_network_imports` 锁);**`verify_draft_card(card, session) -> dict` 5 字段 summary** (spec §7)— `claims_total`(draft_bullets 非空 str 数) / `claims_supported`(至少量化或 slot 文本来源命中) / `low_confidence_claims`(涉及 confidence<0.6 slot 的 bullet 数) / `unsupported_claims`(无任何来源的 bullet 数) / `warnings`(list[str] 含 bullet 索引 + 前 30 字摘要 + 低置信度 slot 名);**双源命中规则** — 一个 bullet 算 supported 当且仅当: 量化数字 regex `(\d+(?:\.\d+)?)\s*(人|%|倍|小时|天|次|万|个|条|例)` 在 captured_slots 任一字符串里出现 OR bullet 是 SOURCE_SLOT_KEYS (`responsibility`/`action`/`result`/`method`/`metric`) 任一 slot 字符串的子串;`compute_confidence_notes(session) -> list[str]` 从 session.slot_meta 聚合 confidence < `INTERVIEW_LOW_CONFIDENCE_THRESHOLD` (0.6) slot 名 + "X 槽位置信度偏低, 保存前请确认" 提示;**`interview_agent.py` 接入改动** — `_do_draft_now` 在 `build_draft_card` 之后调 `verify_draft_card` + `compute_confidence_notes`, 把 verification(5 字段)+ confidence_notes 注入到 `sess.draft_card` dict(供 DraftResponse 返回), 同时缓存到 `sess.verification_summary` 供 `save_card` 写 `_interview_meta.verification`; `api/interview.py` `/draft` endpoint 同步注入, `DraftResponse.draft_card` 现在含 verification + confidence_notes (老前端不消费也不报错); `save_card` 当 `session.verification_summary` 是 dict 时, 写入 `_interview_meta.verification`(5 spec §5.3 schema 限定: `claims_total` / `claims_supported` / `low_confidence_claims` / `unsupported_claims` / `warnings`) + `extraction_mode`(取 `session.interview_mode` 字面量 "rules"/"llm_assisted");**老路径字节级一致** — `verification_summary=None` 时 `save_card` 跳过 verification + extraction_mode(测试 `test_save_card_no_verification_when_draft_not_called` 锁);**隐私边界** (spec §7 + AGENTS.md)— `verify_draft_card` 输出 / `_interview_meta.verification` / DraftResponse 4 层链路**绝不**含 user_message 明文 / source_span 明文 / draft_bullets 完整原文 / draft_card 原文 / API key 值 / env var 名(`LLM_API_KEY`)/ key 前缀(`sk-`)/ `Bearer` 字样 / confidence 数字 / jd_text / prompt 正文;warning 截前 30 字 + draft_bullets[N] 索引, 不复制完整 bullet 文本(`_WARNING_BULLET_PREVIEW_LEN = 30`);**不挂 pre-push hook**;**不引入新依赖**(纯 stdlib `re` + dataclass + 已有 dict 操作);**不修改** `core/llm_rewriter.py` / `core/agent_workflow.py` / `core/agent_tools.py` / `core/tool_schema.py` / `core/interview_prompts.py` / `core/jd_parser.py` / `core/generator.py`;启用 LLM 路径也不通过 verifier 间接触发 LLM 调用(verifier 始终 rules);**31 个 R6-B Phase 4 新 pytest** = `tests/test_interview_verifier.py` (24 case 新文件: TestQuantitativeSource 4 — 命中 / 不命中 / regex 单位 / 无量化走文本分支; TestSlotTextSource 3 — action 子串 / responsibility 子串 / 不命中; TestLowConfidenceClaims 4 — slot_meta<0.6 / 阈值 0.6 不算 / 全高无 notes / claims 计数; TestUnsupportedWarning 2 — 索引 + 前 30 字 / 不含完整 bullet; TestVerifierReturnShape 3 — 5 字段类型 / 空 bullets / 异常输入不抛; TestPrivacyGuarantee 4 — 不含 user_message / 不含 source_span 明文 / 不含 API key / PII 截断; TestVerifierPurity 2 — 不 mutate session / 决定性; TestVerifierNoNetwork 2 — AST 静态扫描 + 不读 env) + `tests/test_interview_api.py` (新增 7 case — TestDraftVerification 4: /draft 含 verification + confidence_notes / 低置信度触发 confidence_notes / 不泄漏 capture_slots 完整原文 / 不含 API key Bearer; TestSaveCardVerificationMeta 3: _interview_meta.verification 含 4 数字 + warnings + extraction_mode / verification_summary=None 不写 meta / 不含 draft_card 原文或 user_message 明文);baseline 809 → **840 pytest 全绿** (31 新增, 809 老测试零回退);**实测耗时** — prompt 4 验证 `pytest tests/test_interview_verifier.py tests/test_interview_api.py tests/test_interview_agent.py` 122 passed / 0.92s;全量 `pytest tests/` 840 passed / 106.97s (含 frontend build `vue-tsc --noEmit` 0 error + `npm run build` 成功);

- **R6-A Phase 1+2 起：JD-driven interview agent + save-card 写库闭环 锁死** —— 来自 `.harness/docs/round6-a-jd-interview-agent-plan.md` Phase 1+2 实施;**核心边界** — `backend/core/interview_agent.py` 不 import `core.llm_rewriter` / `core.agent_workflow` / `core.agent_tools` / `core.evidence` / `core.tool_schema` / `core.session`(R5-E 字节级稳定);只用 `core.jd_parser.parse_jd` / `core.jd_parser.match_score` / `core.generator.load_materials` / `core.logger.log_agent_trace_jsonl` 4 个公开符号;`_INTERVIEW_SESSIONS` 进程内独立 dict 与 `core.session._SESSIONS` 隔离;trace 走 `core.logger.log_agent_trace_jsonl`,`workflow="interview"` + 数字 `step` + `tool` enum `"gap_select"` / `"slot_extract"` / `"draft_card"` / `"save_card"`,`input_size` / `output_size` 只算字节数,不存 user_message / draft_card / edited_card 原文
  - **Phase 1 核心**(规则版缺口选择 + 状态机 + 槽位抽取 + draft_card):
    - 4 个固定 gap(`process_metric` / `tech_metric` / `communication` / `domain_x`),通过 `_score_gap()` 规则打分:`+4 tier=required` / `+2 preferred` / `+1 bonus` / `+3 gap.keywords 命中 missing_keywords` / `+2 同类 ≥2 关键词命中 raw_keywords` / `+2 INTERVIEWABLE_GAP_IDS 白名单` / `-5 NON_INTERVIEWABLE_GAP_IDS 白名单(学历/年限/证书/硬技能/无相邻证据)`
    - session_id `"ia" + 8 hex`(uuid.uuid4().hex[:8]);`InterviewState` enum `EMPTY / DIAGNOSING / ASKING / DRAFT_READY / SAVED / ABORTED`;`ActionType` enum `answer / skip_question / rephrase_question / switch_gap / draft_now`
    - 槽位抽取纯规则:`background` / `responsibility` / `difficulty` / `result` 返单 string;`action` / `method` / `metric` 返 list;未命中关键词 → 加 warning "未识别 X, 已存原文供用户编辑";**不调 LLM**(Phase 4 才接入)
    - 收束条件 3 组合(`background+action+result` / `responsibility+action+metric` / `responsibility+action+result`);`MAX_TURNS_PER_GAP=3` + `MAX_CONSECUTIVE_SKIPS=2`;连续 2 次 skip 强制 DRAFT_READY
    - `build_draft_card()` 返 dict:`title / target_role / source_gap_id / background / responsibility / actions / methods / difficulty / result / metrics / skills / draft_bullets / warnings`(draft_bullets 每条 ≤200 字)
    - API: `POST /api/interview/start` 422 (jd_text 空/>50k/role 未启用) / `POST /api/interview/reply` 422/404/400 / `POST /api/interview/draft` 400(can_draft=False)
    - **33 个 R6-A Phase 1 新 pytest** = `tests/test_interview_agent.py`(TestSessionLifecycle 3 + TestGapSelection 6 + TestSlotExtractionRules 5 + TestDraftCard 4 + TestStateMachine 3 + TestTracePrivacy 2 = 23 case)+ `tests/test_interview_api.py`(TestStartEndpoint 4 + TestReplyEndpoint 4 + TestDraftEndpoint 2 = 10 case)
  - **Phase 2 核心**(save-card 写库 + 刷新闭环,决策点 D1 落地点):
    - `backend/core/interview_agent.py` 新增 `save_card(session, edited_card, save_mode, *, materials_path=None)` 函数 + `DEFAULT_MATERIALS_PATH = Path(__file__).parent.parent / "data" / "materials.json"` 模块级常量 + `_load_materials_for_save` / `_atomic_save_materials`(tmp + os.replace 防半写)/ `_validate_edited_card` / `_generate_project_id` 私有 helpers
    - **新 project schema**:`{id: "interview_YYYYMMDD_NNN", name, period: "", role, category: "interview_captured", summary, highlights: {session.target_role: edited_card["draft_bullets"], "general": edited_card["draft_bullets"]}(双写入, 换 role 不丢), tags: ["interview_agent"] + skills, _interview_meta: {source_gap_id, source_session_id, created_at, warnings}}` — 跟现有 materials.json 兼容(`_pick_highlights` dict schema 字节级锁)
    - `save_mode` Phase 2 只接受 `"append_project"`,其他 ValueError → 400;edited_card 缺字段 / draft_bullets 空 / 超 200 字 → 422
    - **不 import `api.materials._load/_save`** 私有 helper(避免 API 私有 helper 泄漏到 core 层),`core/interview_agent.py` 自带读写 helper
    - **不修改 `PUT /api/materials`** 既有路径,新功能走 `POST /api/interview/save-card` 独立端点
    - **原子写**:`tmp file + os.replace`(plan §2.3 步骤 8);半写不会破坏原文件
    - **trace**:`workflow="interview"` + 数字 `step=(turn_count or 0) + 10`(续接 slot_extract 之后) + `tool="save_card"` + `input_size=edited_card 字节数` + `output_size=new_project 字节数` + 不存 edited_card 原文
    - **隐私边界**(spec §12 + AGENTS.md):测试和冒烟**必须**用临时 `materials_path`(monkeypatch `interview_agent.DEFAULT_MATERIALS_PATH` 指向 tmp_path);生产默认 `backend/data/materials.json`;`git diff` 不应包含真实 materials.json 内容变化
    - **7 个 R6-A Phase 2 新 pytest** = `tests/test_interview_agent.py`(TestSaveCard 3: writes-to-temp-materials-path + generates-unique-id + round-trip-through-preview-resume) + `tests/test_interview_api.py`(TestSaveCardEndpoint 4: happy-path / invalid-save-mode-400 / missing-required-field-422 / empty-bullets-422);**核心不变量锁** — `test_save_card_writes_to_temp_materials_path` 末尾断言真实 `data/materials.json` 内容字节级未变;`test_save_card_round_trip_through_preview_resume` 末尾断言 sentinel bullet 不在真实 materials.json 中
    - **round-trip 自检**:`save_card` 写入后调 `preview_resume(target_role, custom_project_ids=[new_id])` 断言 sentinel bullet 出现在 `project_group.content.projects[*].content.highlights` 中(覆盖到 _pick_highlights 真实选中路径)
  - 683 老测试零回退;R5-E 老路径 `enable_agent_workflow=False` 字节级一致(`from core.llm_rewriter import SYSTEM_PROMPT, _select_system_prompt; assert _select_system_prompt(None, None) is SYSTEM_PROMPT` 仍成立);不改 `PROMPT_VERSIONS` / 不挂 pre-push / 不引入新 LLM 调用 / 不引入新依赖(纯 stdlib)
  - **R6-A Phase 3 起:前端 chat panel + 桌面/移动端 UX 锁死** —— 来自 `.harness/docs/round6-a-jd-interview-agent-plan.md` Phase 3 实施;**核心边界** — `frontend/src/components/InterviewAgentPanel.vue` 不 import `AgentSummary` / `EvidenceSummary` / `ExternalResumePerspective` / `BulletEvaluation` 等 R5 workflow 诊断字段,仅消费 `frontend/src/api/index.ts` 自己新增的 5 个 `InterviewXxx` 类型(隔离 spec §3.3);`App.vue` 加 `.app-shell` flex 布局 + 380px sticky sidecar(桌面) + 右下 FAB + `el-drawer` 全屏聊天(移动端 ≤768px);**0 新 pytest** —— 前端无 Vitest 配置,Phase 3 验收靠 `vue-tsc --noEmit 0 error` + `npm run build` 成功 + 浏览器级 UX smoke(桌面 1280x800 + 移动 375x812 截图留档 `.planning/round6-a-ux/`);**不修改** 后端 `interview_agent.py` / `interview_prompts.py` / `interview.py` / 任何 `core/` 模块 / `scripts/`;`enable_agent_workflow=False` 老路径字节级一致;baseline 723 pytest 零回退(纯前端改动)
  - **R6-A Phase 4 起:LLM slot 抽取增强(2026-06-30 落地)** —— 来自 `.harness/docs/round6-a-jd-interview-agent-plan.md` §4 实施;`extract_slots` 新签名加 keyword-only `llm_enabled` / `llm_api_key` / `llm_base_url` / `llm_model` 4 参数,默认 `False`/None 走规则版(Phase 1 字节级一致);LLM 路径走 stdlib urllib POST `/chat/completions`(跟 R5-E Phase 3 `_call_judge` / R5-D `evaluate_agent_workflow` 同源,不通过 `rewrite_highlights`,不污染 prompt versioning 链路);**LLM prompt 走 `core.interview_prompts.SLOT_EXTRACTION_SYSTEM_PROMPT` / `SLOT_EXTRACTION_USER_TEMPLATE` 独立常量,`USER_TEMPLATE` 只含 `{slot}` + `{user_message}`,**不**含 `{jd_text}`**(隐私边界);**LLM prompt 不进 `PROMPT_VERSIONS`**(决策点 D5,验证: `assert "v6-interview-slot" not in PROMPT_VERSIONS` 通过);**R5-E 边界** — `backend/core/interview_agent.py` 文件任意位置不出现 `from core.llm_rewriter import` / `import core.llm_rewriter`(现有 `TestGapSelection::test_select_gap_does_not_import_workflow` 用正则锁,撞到 lazy import 后改为本地常量 `_INTERVIEW_LLM_DEFAULT_BASE_URL` / `_INTERVIEW_LLM_DEFAULT_MODEL` 同步 `core.llm_rewriter.DEFAULT_*` 的值,测试 `test_interview_llm_defaults_match_llm_rewriter` 防漂移);**fallback 设计** — 网络错(URLError/TimeoutError/HTTPError)不 retry → fallback 规则版;JSON 失败 / schema 失败 → retry 1 次(strict_retry=True,沿用 `_call_with_retry` 模式),仍失败 fallback 规则版,不抛(spec §4.4 "失败不阻断主流程");**隐私边界** — api_key 只走 env `LLM_API_KEY` / 显式入参,**永不**进 `extract_slots` 返回值 / trace / 日志 / 配置 dict(`_resolve_interview_llm_config` 返回 4 字段 `enabled_for_call` / `llm_enabled` / `model` / `base_url`,**不**含 key);user_payload 只含 `{slot, user_message, instructions}`,**不**含 jd_text / session / materials;**不挂 pre-push**;**不引入新依赖**(纯 stdlib `urllib` + `json` + `os` + 已有 `dataclass`);**不修改** `core/llm_rewriter.py` / `core/agent_workflow.py` / `scripts/`;baseline 729 → **739 pytest 全绿**(+10 新 pytest,729 老测试零回退);**10 个 R6-A Phase 4 新 pytest** = `tests/test_interview_agent.py`(TestLLMSlotExtraction 6: disabled-fallback / no-key-fallback / with-key-calls-LLM(mock urlopen 调 1 次,LLM 输出透传) / invalid-json-retries-once(失败 retry 1 次,urlopen 调 2 次) / schema-error-fallback(缺 action key,fallback 规则,urlopen 调 2 次) / network-error-fallback(URLError 兜底,urlopen 调 1 次) + TestInterviewPromptRegistry 4: prompt-registered(非空 str 锁) / template-excludes-jd-text(隐私边界锁) / PROMPT_VERSIONS-clean(R5-E 边界锁,4 个 forbidden key 全部 assert) / local-defaults-sync-llm_rewriter(防本地常量漂移));Phase 5 eval 启动条件从 0/3 推进到 1/3(Phase 4 上线 ✅)
  - **R6-A Phase 5 起:eval 脚手架 + 规则版基线(plan §5.1 启动条件 1/3 满足,仍 deferred work)** —— 来自 `.harness/docs/round6-a-jd-interview-agent-plan.md` Phase 5 实施;**核心边界** — `scripts/evaluate_interview_agent.py` **不修改** `backend/core/interview_agent.py` 任何字节(跟 Phase 1+2+3 R5-E 保护一致);**不 import** `core.llm_rewriter` / `core.agent_workflow` / `core.agent_tools` / `core.evidence` / `core.tool_schema` / `core.session` 禁列模块;**只 import** `core.interview_agent` 4 个公开符号(`create_session` / `apply_action` / `build_draft_card` / `extract_slots`)+ `core.jd_parser.parse_jd` + `core.generator.load_materials` + `core.llm_rewriter.is_llm_enabled`;复用 R5-D `scripts/evaluate_agent_workflow.py` 的 6 个 helper(`_resolve_eval_mode` / `_get_llm_eval_config` / `_check_pii_safe` / `_percentile` / `MODE_OFFLINE` / `VALID_EVAL_MODES`);**live mode 在脚本内显式拒绝**(`if resolved_mode == MODE_LIVE: print("[error] --mode live 当前不可用: Phase 5 需用户跑 10+ 轮真实面试数据后再启 live 对照"; return 2)`)防止误启无对照 live eval;**offline 模式**当前 eval set 实测 `total=10`(来源为 plan_baseline / simulated_user_v1 / 补充边界样本的合并集) → 报告默认写 `backend/logs/interview_eval_report.md`(.gitignore),7 章节只含聚合指标 + slot key/长度/计数,不含 user_message / draft_card 原文 / API key / 真实 PII;**隐私自检** = report 渲染后 `_check_pii_safe(row + metrics)` 二次扫 + 抽样 10 条 user_message 原文 grep 防泄漏(`test_report_contains_no_user_message_text`);**6 个 R6-A Phase 5 新 pytest** = `tests/test_interview_eval.py`(TestEvalHelpers 4: extract_slots_iteratively / schema_pass_rate / completeness / fabrication_guard 单元;TestEvalReport 2: 报告不含 user_message 原文 / offline 模式不调 urlopen);**不挂 pre-push**;**不引入新 LLM 调用 / 不引入新依赖**(纯 stdlib + dataclass + 既有 helper);**不修改** `evaluate_agent_workflow.py` / `evaluate_prompt_versions.py` / `match_score` / `score_thresholds.py` / `match_golden_targets.py` / `replay_agent_trace.py` / `requirements.txt`;baseline 723 → 729(+6 pytest, 723 老测试零回退);**plan §5.1 启动条件** (Phase 4 LLM 抽取上线 ✅ / 真实 LLM key 接入 ❌ / 用户跑 10+ 轮真实面试 ❌) **当前 1/3 满足**,Phase 5 仍作脚手架验证 + 规则版 baseline,报告**不做**决策依据;等用户在 chat panel 跑 10+ 轮真实对话后,再跑一次 `scripts/evaluate_interview_agent.py --mode offline` 生成 v2 报告(real + simulated)作 Phase 4 启动的 ground truth
  - **R6-B Phase 0 起:基线校准 + 口径修正(2026-06-30 落地)** —— 来自 `.harness/docs/round6-b-interview-agent-intelligence-spec.md` §0+§1.1+§15 Prompt 0 实施;R6-B 真正的增量是"可信增强层"(slot_meta provenance / API mode 开关 / deterministic policy / draft verifier / eval compare / 前端最小呈现),**不**重做 LLM 抽取,也**不**把面试官改成自由聊天 Agent;**Phase 0 只做口径校准** — `scripts/evaluate_interview_agent.py` 中仍写着"Phase 4 LLM 未上线"的注释、live mode 错误信息、报告主标题 / §二 / §四 标题口径全部更新为"R6-A Phase 4 LLM slot extraction 已上线但本报告仅跑 rules baseline"对应表述,脚本行为零变化(offline 仍跑通 10 条样本 + total schema_pass_rate=0.30 / avg_completeness=0.53 / fabric_violations=0,live mode 仍 `return 2` 拒绝);**核心边界** — **不**新增 LLM 调用 / **不**修改 `core.agent_workflow.py` / `core.agent_tools.py` / `core.tool_schema.py` / `PROMPT_VERSIONS` / **不**写真实 `backend/data/materials.json` / **不**挂 pre-push;**0 新 pytest** — Phase 0 是纯口径修正,baseline 维持 739(`tests/test_interview_api.py::TestDraftEndpoint` 含 `37ad00c fix(round6-a): set draft endpoint state to draft ready` 回归 + `tests/test_interview_agent.py::TestLLMSlotExtraction` 6 + `TestInterviewPromptRegistry` 4 + `tests/test_interview_eval.py` 8 = 18 个验证用例 + 全量 739 pytest 零回退);**R6-B Phase 1 起顺序**:Phase 1 slot_meta provenance → Phase 2 API mode 开关 → Phase 5 eval compare(先补 provenance + API mode + eval compare,确认 llm_assisted 有数据收益后再上 Phase 3 policy / Phase 4 verifier / Phase 6 frontend);**R6-B 不**做 — 自由聊天助手 / 模拟面试训练 / 自动投递 / 账号云端多用户 / 向量数据库 Redis 后台队列 / 完整对话持久化 / LLM 自动保存素材 / 重构 LLM slot extraction 到新模块 / 改默认 resume rewrite prompt / rollout R5-E prompt winner / live eval report 自动入库(spec §13 非目标清单)
- R5-E Phase 3 起活跃基线 **683 个用例** (历史快照 — R6-A Phase 5 起改为 729):计算口径沿用 R5-D 596 + R5-E Phase 1 + Phase 2 + Phase 3:R5-E Phase 1 +31 NEW FILE `test_prompt_versioning.py`(PROMPT_VERSIONS 字节级锁死 + 4 version 可选 + API/workflow 透传 + 错误信息不含 prompt 正文)+ R5-E Phase 2 +28 NEW FILE `test_prompt_eval.py`(evaluate_prompt_versions.py A/B 评测脚本: offline 模式无 key 出报告 / 报告含 4 version + by-version 聚合 / 报告不含 prompt / JD / bullet / API key / unknown version 拒绝不泄漏)+ R5-E Phase 3 +28 in `test_prompt_eval.py`(judge helper / schema 校验 / 网络失败 / JSON 错误 / offline 强制 disabled) = **683 passed + 0 skipped**(R5-E Phase 3 起)
  - 跑：`cd backend && D:\python3.11\python.exe -m pytest tests/ -v`
  - 新增行为必须有 pytest 覆盖（核心逻辑 / 边界 / 集成），thin wrapper / URL 字面量 / mock 自指 → 不写
  - **每轮独立验证 + 清理冗余测试**：跑全量绿后审视新增文件是否冗余
  - **bugfix 必须加回归测试**：覆盖每个 score 区间 + 每个死代码删除点,防止回潮
  - **R3-I 起：6 role 字节级 hash baseline 锁死** —— 不传 JD 时 `build_sections(target_role)` 输出必须与固化 hash 一致,任何 baseline 漂移立刻 fail,防止后续 round 不小心改默认排序路径
  - **R3.5 起：阈值 80/60 锁死 + 6 份 ground truth 验证** —— `tests/test_threshold_tuning.py` 锁 `_classify_recommendation` 在 `≥80 高 / ≥60 中 / 否则低`;基于 `简历帮知识库/jd_samples.json` 8 份 ground truth（排除 2 份公告型）跑 match_score 断言分类正确
  - **R3.5+ 起：borrowed pool + 'AI' surface 锁死** —— `match_score(text, role, materials, include_borrowed=True)` 默认开 borrowed pool（全素材库扫描，缓解跨 role false negative）；`KEYWORD_GROUPS['skills']` 加 `('AI', 'LLM', 0.5)`；`TestMatchScoreBugfixR35Plus` 3 个测试覆盖 baiyun_qa / baiyun_product 修后 score>0 + include_borrowed=False 旧行为保留
  - **R3.5+ (b) 起：PM 维度 surface 锁死** —— `KEYWORD_GROUPS['domains']` 加 4 个 PM 维度 surface（物流/工业工程/原型/流程图，0.5 加分）让 match_score 精确识别 baiyun_2026_product 等 PM 岗位缺失;suggestions 给"补 PM 维度素材"指引;`TestMatchScorePMDimensions` 3 个测试锁死 missing 含 PM 4 项 + suggestions 提到 PM 关键词 + KEYWORD_GROUPS 字典级断言
  - **R3.5.1 起：score_thresholds.py 实跑模式锁死** —— `scripts/score_thresholds.py` 跑 `match_score(text, role_id_hint, materials)` 拿 score / coverage, 不再读 jd_samples.json frozen top_score;`tests/test_score_thresholds.py::TestScoreThresholdsLive` 5 个测试锁死 (含 `test_live_mode_ignores_frozen_top_score` 篡改 frozen 字段验证实跑分数不变, 核心防回潮)
  - **R3-M.2 起：8 套模板可读性参数 + academic 专属 renderer 锁死** —— LAYOUT_CONFIG 8 套必含 h1_size_ratio / h2_size_ratio / section_spacing_pt(2 元组) / meta_spacing_pt / item_spacing_pt;`TestLayoutConfigSchema` 5 个 case 锁 schema;`_render_academic` 走 _LAYOUT_DISPATCH['academic'] 且项目 highlights 简化(无 H2 项目名 / 无独立 period meta / 无 summary,直接列 highlights 为 bullets);`TestReadabilityAcrossLayouts` 4 个 case 锁 body>=9pt / meta>=8pt / 4 间距>=0 / line_spacing [1.15, 1.5]
  - **R3-M.3 起：bilingual_mode 激活 + academic_layout 详细模式锁死** —— `_LAYOUT_DISPATCH['bilingual']` 切到 `_render_bilingual`(3 个 bilingual section helper:`_render_bilingual_header_to` / `_render_bilingual_education_to` / `_render_bilingual_project_group_to`;`name_en` / `school_en` / `major_en` / `title_en` 缺失时 graceful 降级到单语言);`_render_academic` 按 `academic_layout` 分支 dispatch(compact → `_render_project_group_academic_to` 默认,detailed → `_render_project_group_academic_detailed_to` 恢复 H2 + meta + summary);`TestAcademicLayout` 6 case 锁分支行为 + `TestBilingual*` 12 case 锁双语 + 真实数据 graceful + `TestMaterialsBilingualSchema` 2 case 锁 build_sections schema 透传;`PreviewRequest` / `GenerateRequest` 加 `academic_layout` 字段,`render_docx` 拷贝 LAYOUT_CONFIG 后覆盖(不污染全局)
  - **R3-P 起：Prompt 工程升级 + 显式 schema + 失败 retry 锁死** —— `SYSTEM_PROMPT` v2 含 2 个 few-shot 示例(基础改写 + jd_focus 改写)+ 7 条硬性约束(不编造 / 长度 20-50 字 / JSON schema 唯一 / jd_focus 4 条);显式 JSON schema `{rewritten: [{index: 0..N-1, text: "..."}]}` 唯一规范(index 必唯一 + 0..N-1 + non-empty str);`_call_with_retry` 解析失败 retry 1 次(`_build_request_payload` 加 `strict_retry=True` 强约束提示),网络错误不 retry(避免 token 浪费);旧 schema(顶层 array / `{rewritten: [str]}`)保留兼容(16 老 case 全绿);`TestSystemPromptV2` 3 + `TestNewSchemaExtraction` 5 + `TestRetryOnInvalid` 3 + `TestSchemaValidationUnit` 8 = 19 case 锁死
  - **R4-F 起：Function Calling 协议接入锁死** —— `TOOL_EVALUATE_SCHEMA` OpenAI tools schema + `evaluate_bullet_jd_match(bullet, jd_focus)` 工具函数(从 match_score 内部抽 surface 扫描逻辑);`rewrite_highlights` 加 `enable_function_calling: bool = False` 参数(默认关,旧路径字节级一致);`jd_focus is not None` 时挂 tools,空时走老路径;`PreviewRequest` / `GenerateRequest` 加同名字段透传;`TestFunctionCalling` 5 case 锁 tools 字段结构 / 默认不挂 / jd_focus=None 不挂 / 启用挂载 / tool_calls 解析失败降级原文 + `TestEvaluateBulletJdMatch` 3 case 锁新工具函数 surface 命中 + missing 列表
  - **R4-A 起：Agent Loop (max_step=3) + 单工具约束 + trace 日志锁死** —— `MAX_AGENT_STEPS=3` 硬上限防死循环 + 单步单工具防 token 爆 + 网络错误不进入 loop(直接降级不浪费 token);`enable_function_calling=False` 时完全不走 loop(字节级一致,旧测试不破);`logger.log_agent_trace(session_id, step, tool_name, latency_ms, outcome)` 写 `logs/agent_trace.log` 跟 `generation.log` 分离;`TestAgentLoop` 9 case 锁 max_step 上限 / 工具成功路径 / 工具失败回退 / 连续 3 步仍无 output 降级 / trace 格式 / 网络错误不入 loop / max_step=0 走老路径 / 单步单工具约束 / execute_tool_call known+unknown 工具 + `TestLogAgentTrace` 3 case 锁 trace 写入格式 + 文件创建 + 字段完整性
  - **R4-M 起：Session 记忆(进程内 deque 上限 10) + 隐私隔离锁死** —— `core/session.py` `_SESSIONS: dict[str, deque(maxlen=10)]` + 4 API(create_session / get_messages / append_message / clear_session);`rewrite_highlights` 加 `session_id: str | None = None` 参数,挂上后从 session 拉历史 messages 拼到 LLM messages 头部;`PreviewRequest` / `GenerateRequest` 加 `session_id` 字段透传;**隐私隔离** — `session_id` 由前端生成(不存 PII),后端不写 session 内容到日志(只写 session_id + 步数);MVP 不持久化(进程退出即丢),不做 TTL;`TestSessionAPI` 8 case 锁 create/append/get/clear/上限 10 FIFO/不存在 session_id 静默忽略/空 session_id 静默忽略/唯一性 + `TestSessionIntegration` 3 case 锁 session_id 拼接到 LLM messages / session=None 不拼接 / 多轮同 session 累积
  - **R4-M integration 起：session_id 整链透传锁死** —— `rewrite_highlights` → `generator.build_sections` → `api/resume.py` Preview/Generate 整链 `session_id` 字段透传(零修改上层行为,`session_id=None` 旧路径字节级一致);cherry-pick R4-M 后必须补 `TestSessionIntegration` 链路验证
  - **R5-A(Phase 1)起：Agent 编排层与工具注册锁死** —— `core/agent_tools.py` AGENT_TOOLS 含 `parse_jd` / `match_score` / `evaluate_bullet_jd_match` / `rewrite_highlights` 4 个核心工具;`execute_agent_tool(tool_name, args, context)` 统一入口:未知工具 → `ToolErrorType.TOOL_NOT_ALLOWED` 不抛 / TypeError → `TOOL_ARGS_INVALID` / 其他异常 → `TOOL_RUNTIME_ERROR`;`ToolResult` dataclass **不存 args / input 原文**(隐私边界 `TestPrivacyGuarantee::test_tool_result_has_no_args_or_input_attrs` 锁);`core/agent_workflow.py` `build_task_graph(has_jd, enable_function_calling, has_external_resume)` 确定性产任务图(LLM 不参与规划)— 同样输入字节级一致 + step 序号连续 + `AgentStep` frozen=True;`run_agent_workflow(...)` 失败时降级到旧路径(走 `build_sections` / `render_docx`),最终输出与 `generator.preview_resume / generate_resume_docx` 结构一致;`enable_agent_workflow=False`(默认)→ `preview_resume` / `generate_resume_docx` 字节级一致(R4-M 283 老测试零回退);`PreviewRequest.enable_agent_workflow` / `GenerateRequest.enable_agent_workflow` 字段顺序在 `session_id` 之后新增,前面既有字段顺序不动(Pydantic 向后兼容)
- **R5-A(Phase 2)起：结构化 JSONL trace + 会话回放锁死** —— `core/logger.py` `JSONL_TRACE_FIELDS` 11 字段稳定 schema(ts / request_id / session_id / workflow / step / tool / latency_ms / status / error_type / input_size / output_size);`log_agent_trace_jsonl(event)` 写 `backend/logs/agent_trace.jsonl`,写入失败(IO / 磁盘满 / 编码错)由 logger 内部 try/except 静默降级不影响主流程(spec §6.3);`core/agent_workflow.py` 每次 workflow 生成 request_id(短 uuid,前缀 "r"),每个 step 含本地步骤(intent / retrieve / aggregate / parse_external_resume)写一条 trace(`status="skipped"` 表示本地步骤);`input_size` / `output_size` 只算 bytes 长度不存原文(`_estimate_input_size` / `_estimate_output_size` 用 `json.dumps(...).encode("utf-8")` 算字节);`generate_request_id()` 模块级函数供调用;旧 R4-A `log_agent_trace` / `agent_trace.log` 签名/格式/路径完全不动,两个文件并存;`scripts/replay_agent_trace.py` argparse CLI `--request-id` / `--session-id` / `--path`,输出 markdown 摘要只渲染 7 列(step/tool/latency_ms/status/error_type/input_size/output_size),不输出 event 整体 dict / 任何原文;`TestLogAgentTraceJsonlSchema` 4 + `TestLogAgentTraceJsonlTypes` 3 + `TestLogAgentTraceJsonlPrivacy` 2 + `TestLogAgentTraceJsonlRobustness` 3 + `TestWorkflowJsonlTrace` 9 + `TestReplayFilter` 3 + `TestReplayMarkdown` 3 + `TestReplayScript` 3 + `TestReplayRobustness` 2 = **32 新 pytest** 全绿(320 → 352);**安全审查无 P0/P1 阻塞**
- **R5-A Phase 3 起：轻量 RAG evidence 锁死** —— `core/evidence.py` `EvidenceSnippet` frozen dataclass(source_type/source_id/text/matched_keywords/confidence) + `build_evidence_snippets(materials, role)` 从 projects/skills/honors/certs 4 类切 snippets(role→general fallback) + `retrieve_evidence(jd_keywords, role, materials, top_k=8, min_confidence=0.0)` 复用 `KEYWORD_GROUPS` surface/normalized 做 lexical retrieval + 排序稳定 `(-confidence, source_type, source_id)` + 0 命中 snippet 过滤(不返噪声);`_summarize_evidence_for_prompt` 单条 80 字符截断 + 总 2000 字符上限 + `(N more)` 截断标记;`evidence_to_dict_list` 把 frozen dataclass 转 JSON 友好 dict list(tuple→list,round 3 位小数);`core/agent_tools.py` 注册 `retrieve_evidence`(`ToolSpec.pii_risk=medium` 跟 match_score 同级,callable 包 lambda 走 `evidence_to_dict_list`);`core/agent_workflow.py` has_jd 时任务图插入 `retrieve_evidence` step(match_score 之后,retrieve_materials 之前,step.required=False,fallback="use_default"),evidence 失败不阻断主流程;caller 显式传 `evidence` kwarg 时 retrieve_evidence 工具调用 `status="skipped"` 跳过执行;`enable_agent_workflow=True` 时 preview 返回值新增 `evidence_summary` 字段(dict list);`core/llm_rewriter.py` `rewrite_highlights` 加 `evidence` kwarg(None 字节级一致)+ `_build_request_payload` 加 `evidence_summary` kwarg + `SYSTEM_PROMPT` 加第 8 条硬性约束"只能基于 evidence 中存在的事实改写" + `EVIDENCE_CONSTRAINT_SUFFIX` 单独常量后缀(evidence_summary 非空时追加);`build_sections` / `preview_resume` / `generate_resume_docx` 加 `evidence` kwarg 透传(零修改上层行为,None 旧路径字节级一致);**零向量数据库 / 零 embedding API / 零新依赖**(spec §5.3);`TestBuildEvidenceSnippets` 9 + `TestKeywordHit` 5 + `TestComputeConfidence` 6 + `TestRetrieveEvidence` 9 + `TestSummarizeEvidenceForPrompt` 4 + `TestEvidenceToDictList` 3 + `TestAgentToolsIntegration` 4 + `TestMatchScoreRegression` 4 + `TestKeyWordGroupsReuse` 1 = **45 个 test_evidence 新 pytest** + `TestEvidencePhase3` 9 个 test_agent_workflow 集成(任务图含 retrieve_evidence / workflow 跑 evidence step / evidence_explicit 时 skipped / evidence 失败降级不阻断 / trace 不含 evidence text / evidence_summary 返回 / 无 JD 不调 evidence / jd_context=None baseline 不变 / 无关键词命中返空 list) + `TestEvidenceIntegration` 8 个 test_llm_rewriter evidence payload(evidence=None 字节级一致 / 空 list 等同 None / snippets 注入 summary / dict list 重建 Snippet / system prompt 含 suffix / retry 保留 summary / FC + evidence + jd_focus 三方共存 / `_build_request_payload` kwarg 透传) = **62 新 pytest**(352 → 416 全绿);**安全审查无 P0/P1 阻塞**(trace 不含 evidence text 原文 / evidence_summary 不含 PII / 无新增外部网络调用 / write fail 静默降级不影响主流程);后续 Phase 4(Agent eval 报告)需真实 LLM key 才能产出 schema pass rate / fallback rate 等指标,等用户明确启动再开
- 后端冒烟：`python main.py` 启动后访问 `http://127.0.0.1:8000/api/health` 应返回 `{"status":"ok"}`
- **R5-A Phase 4 起：Agent 离线评测报告 锁死** —— `scripts/evaluate_agent_workflow.py` 跑固定 eval set 12 JD(jd_samples 8 份非公告型 + v4_strong 4 份) × 4 组开关对照 (FC × AW 笛卡尔积),输出 markdown 报告 `AI岗位JD库_agent_eval报告.md`(8 节: eval set 概览 / 四组对照总览 / score 一致性 / 每 JD 工具调用摘要 / 失败 case / 隐私检查 / 结论 / 与既有脚本关系);无 LLM key 时全部走原文 fallback,脚本不报错;**不挂 pre-push hook**(spec §12 #3 默认手动脚本);**不修改既有 match_score / score_thresholds.py / match_golden_targets.py / replay_agent_trace.py**;PII 检查用 placeholder 白名单 (`13800000000` / `your_email@example.com`) 容忍公开脱敏版 demo;`TestEvalSetLoading` 3 + `TestSingleEvaluation` 4 + `TestPiiScanner` 2 + `TestPrivacyGuarantee` 2 = **11 个 test_agent_eval 新 pytest**(416 → 427 全绿,416 老测试零回退);**安全审查无 P0/P1 阻塞** — 报告不含真实 PII / 不读 private 备份 / 无外部网络依赖
- **R5-A closeout 起：4 个 bug 修复锁死** —— 来自 `.planning/agent-architecture-audit/findings.md` 9 个 gap + 隐性 bug;(A) `enable_external_resume: bool = False` 字段透传 (api/resume.py + core/generator.py + core/agent_workflow.py 整链), 默认 False 字节级一致;`build_task_graph(has_external_resume=True)` 时任务图加 `parse_external_resume` step (P2 占位, tool=None, required=False, fallback="skip");(B) workflow preview 返回值新增 `agent_summary` 字典 (5 字段: request_id `r[0-9a-f]{8}` / steps_executed / tools_used / fallback_used / latency_ms), 老路径 (enable_agent_workflow=False) **不含** agent_summary 字段 (字节级一致);(C) `core/agent_tools.py` `_validate_required_args(spec, args)` 在 callable 调用前校验 input_schema.required 字段存在性 (不强校验 type, 避免破坏); 缺字段返 `ToolResult(status="error", error_type=TOOL_ARGS_INVALID, latency_ms=0)` 含缺失字段名 (无 args 原文);(D) 隐性 bug 修复: `match_score` schema 字段名 `role` → `target_role` 对齐 `core.jd_parser.match_score(text, target_role, materials, ...)` 真实函数签名, `agent_workflow._build_tool_args` 同步改 `target_role` (三方对齐: schema / 函数签名 / workflow 调用); 修复原因: Phase 1 设计是工具失败不阻断主流程, 所以 workflow 调 match_score 抛 TypeError 被兜底成 TOOL_ARGS_INVALID 但 build_sections 仍用本地变量独立算 score, 输出正确但工具日志始终 error (隐性 bug);**14 个 test_agent_workflow closeout 新 pytest** = `TestEnableExternalResumePassthrough` 3 + `TestAgentSummaryField` 7 (含老路径不含 agent_summary 字节级一致锁) + `TestAgentToolArgsValidation` 4 (含 schema 字段名防回归 `test_match_score_schema_uses_target_role`); 427 → **441 全绿** (416 baseline + 25 R5-A Phase 1-3 + 14 closeout), 416 老测试零回退;不挂 pre-push hook;不破坏既有 baseline 字节级一致
- **R5-B Phase 2A 起：工具契约 / 权限 / 真实数据流锁死** —— 来自 `.harness/docs/round5-b-agent-capability-spec.md` §3 实施(`core/tool_schema.py` 新模块 — 轻量 JSON schema validator 子集 type / required / properties / items / minimum / maximum, 零依赖纯 stdlib;`core/agent_tools.py` 升级 `_validate_required_args` → 委托 `validate_schema(spec, value)`, 新增 `_check_permission_context(spec, context)` 校验 `allow_jd_text` / `allow_materials` / `allow_external_resume` / `max_pii_risk`, 权限不匹配返 `ToolResult(status="error", error_type=PRIVACY_VIOLATION, latency_ms=0)`, 早于 schema 校验防止敏感数据进入校验日志;`ToolSpec.metadata={"affects_preview": bool}` 新增字段(default `{}` 向后兼容)+ `affects_preview(tool_name)` helper; `retrieve_evidence` 标 `affects_preview=True`(output 真正注入 `build_sections` → `rewrite_highlights` → 影响 highlights); `core/agent_workflow.py` 新增 `_build_step_context(tool_name, has_jd, has_external_resume)`, 任务图每个工具步骤派发 context(JD 路径 `allow_jd_text=True` / `allow_materials=True`, 外部简历步骤 `allow_external_resume=True`); `agent_summary.tools_used` 升级为有效语义 — 只列 `affects_preview=True` 且 `status=success` 的工具, 展示型工具(`parse_jd` / `match_score` / `evaluate_bullet_jd_match` / `rewrite_highlights`)即使跑过也不列; 错误描述只含字段名 + 类型名 + 权限名 不含 args / JD / bullet 原文(spec §6.4 隐私边界); **46 个 R5-B Phase 2A 新 pytest** = `tests/test_tool_schema.py` 19(`TestSchemaRequired` 3 + `TestSchemaType` 5 + `TestSchemaRange` 2 + `TestSchemaArray` 3 + `TestSchemaNoSchema` 2 + `TestSchemaPrivacy` 1 + `TestSchemaIntegration` 3) + `tests/test_agent_tools.py` 18(`TestToolPermissionContext` 8 — allow_jd_text/allow_materials/allow_external_resume/max_pii_risk 拒绝 + 错误描述不含原文 + 权限校验早于 schema 校验 / `TestAffectsPreview` 6 — 5 个工具各自的 affects_preview 元数据 / `TestExecuteAgentToolSchemaIntegration` 4 — type 错 / range 越界 / array items 类型错 / 合法 args 走通) + `tests/test_agent_workflow.py` 9(`TestWorkflowEffectiveTools` 5 — tools_used 只列 retrieve_evidence / 无 JD 路径空 list / 失败 effective 工具不进 tools_used / 老路径不含 agent_summary 字节级一致 / workflow 走新 schema validator / `TestWorkflowContextDispatch` 4 — JD 路径 allow_jd_text=True / 无 JD 路径 allow_jd_text=False / 外部简历路径 allow_external_resume=True / workflow 派发 context); 441 → **487 全绿** (441 R5-A baseline + 46 R5-B Phase 2A 新增), 441 老测试零回退; **不挂 pre-push hook**(spec §12 #3 默认手动脚本); **不引入新 LLM 调用 / 不引入新依赖**(纯 stdlib + dataclass); **不实际消费** external_resume(留 Phase 3); **不把 eval 挂 pre-push**(留 Phase 5); **不实施 GUI Agent 面板**(留 Phase 4 等用户明确启动); **老路径不变**(`enable_agent_workflow=False` 字节级一致 — `agent_summary` 字段只在 workflow 路径加)
- **R5-C Phase 2 起：外部简历进入 Agent workflow 锁死** —— 来自 `.harness/docs/round5-c-agent-capability-spec.md` §3 实施(`PreviewRequest` / `GenerateRequest` 加 `external_resume_text: str | None = None` 字段, 老路径完全忽略 — 字节级一致); `core/agent_workflow.py` `run_agent_workflow` 接受 `external_resume_text` kwarg, `has_external_resume` 以 `external_resume_text` 非空为真实依据(`enable_external_resume` bool 保留向后兼容); 任务图 `has_external_resume=True` 时插入 `parse_external_resume` + `compare_resume_jd` 两步(都在 `retrieve_materials` 之后 / `rewrite_highlights` 之前, spec §3.3, `compare_resume_jd` 仅在 `has_jd` 时加入); `core/jd_parser.py` 新增 `parse_external_resume(text)` — 输出压缩 `{profile: {char_count, paragraph_count}, keywords: list[str]}` 不含段落原文, 与 `compare_resume_jd(text, jd_text, target_role, materials)` — 输出 `{have_keywords, need_keywords, materials_can_cover, resume_only_keywords, suggestions, counts: {have, need, materials_can_cover, resume_only}}` 4 维摘要; `core/agent_tools.py` 注册这两个工具(permission = `read_external_resume` / `read_jd_and_external_resume`, `pii_risk="high"`, `affects_preview=False` 仅作诊断); `_check_permission_context` 新增 `read_jd_and_external_resume` 复合权限分支(同时需 `allow_jd_text=True` + `allow_external_resume=True`); `_build_step_context` 在 `has_external_resume=True` 时 `max_pii_risk="high"` 让外部简历工具可执行(默认 medium); workflow preview 返回值新增 `external_resume_perspective` 字段(对齐 compare_resume_jd 输出 schema, **不含原文**), 老路径(`enable_agent_workflow=False`)完全不含此字段; **隐私边界**(spec §3.4)— `ToolResult.output` 不存原文(parse_external_resume 只返统计 + 关键词, compare_resume_jd 只返 4 维摘要 + counts + suggestions), JSONL trace 只写 `input_size/output_size`, `agent_summary` 不含简历原文, error_msg 不含简历/JD 原文; **17 个 R5-C Phase 2 新 pytest** = `tests/test_r5c_phase2_external_resume.py` (`TestExternalResumeApiField` 2 — PreviewRequest/GenerateRequest 字段透传 + 默认 None / `TestExternalResumeWorkflowUsesText` 3 — text=None/""/非空 三态任务图分支 / `TestParseExternalResumeTool` 2 — schema 稳定 + 输出不含 sentinel / `TestCompareResumeJdTool` 2 — schema 稳定 + 输出不含 sentinel / `TestExternalResumePermission` 1 — `allow_external_resume=False` 返 PRIVACY_VIOLATION 且 error_msg 不含原文 / `TestExternalResumePrivacy` 3 — trace + agent_summary + evidence_summary 三层都不含原文 / `TestExternalResumePreviewOutput` 1 — workflow preview 含 schema 稳定 external_resume_perspective / `TestExternalResumeOldPathUnchanged` 2 — 老路径不含 external_resume_perspective + sections 字节级一致); **baseline 3 个修复** = `tests/test_agent_tools.py::test_each_tool_has_required_fields` 接受 2 个新 permission + `tests/test_agent_workflow.py::test_with_external_resume_adds_parse_step` / `test_true_adds_external_resume_step` 从 `tool=None` 占位改为 `tool="parse_external_resume"` 真工具断言; **不挂 pre-push hook**(spec §12 #3 默认手动脚本); **不引入新 LLM 调用 / 不引入新依赖**(纯 stdlib + dataclass); **不实际消费** external_resume 进改写(留 Phase 3); **不实施 GUI Agent 面板**(留 Phase 4 等用户明确启动); **不破坏老路径字节级一致** — `enable_agent_workflow=False` 路径不含 `external_resume_perspective` 字段
- **R5-C Phase 3 起：per-bullet 真实评估数据流锁死** —— 来自 `.harness/docs/round5-c-agent-capability-spec.md` §4 实施(`core/agent_workflow.py` 新增 `_evaluate_top_bullets(materials, target_role, jd_focus, jd_context, *, top_projects=3, bullets_per_project=3) -> list[dict]` helper — 选 top projects (默认 3, 来自 `ROLE_CONFIG["preferred_project_ids"]`, 有 jd_context 时用 `rank_projects` 按命中数重排, 跟 `build_sections` 同源) × top bullets (默认 3, 来自 `_pick_highlights(proj, target_role)` 前 3 条), 逐条跑 `evaluate_bullet_jd_match` 纯函数聚合, 输出 list[dict] 含 7 字段: `project_id` / `bullet_index` / `matched_keywords` / `missing_keywords` / `matched_count` / `missing_count` / `suggestion`, **不含 bullet 原文**); 任务图 `evaluate_bullet_jd_match` step 触发条件由 `enable_function_calling and has_jd` 改为 **`has_jd`** (不再依赖 FC, spec §4.2 "无 JD 时不执行"); workflow 主循环执行 evaluate step 时**走批量 helper 而非单 representative bullet**, 单 step 1 条 trace (status=success/error, input_size/output_size 反映批量总和); workflow preview 返回值新增 `bullet_evaluations` 字段 (老路径 `enable_agent_workflow=False` 完全不含此字段 — 字节级一致); `evaluate_bullet_jd_match` 工具仍保持 `affects_preview=False` (spec §4.4 — bullet_evaluations 先作为诊断输出, 后续明确用于排序 / 改写时再升 `affects_preview=True`); 无 JD / 项目无 bullets / 批量失败 → `bullet_evaluations = []`; 单条 bullet 评估失败 → 该条用空 matched/missing schema 占位 (spec §6.3 不阻断); `_pick_representative_bullet` 保留 (rewrite_highlights 工具 args 用), 不动; **10 个 R5-C Phase 3 新 pytest** = `tests/test_r5c_phase3_bullet_evaluation.py` (`TestBulletEvaluationSelectsTopProjects` 1 — helper 单元: 3 projects × 3 bullets 截断 + bullet_index 连续 / `TestBulletEvaluationOutputSchema` 2 — workflow preview 含 list[dict] 7 字段类型正确 + 序列化不含 bullet 原文 sentinel / `TestBulletEvaluationNoRawBulletInTrace` 1 — JSONL trace 不含 bullet 原文 sentinel / `TestBulletEvaluationSkippedWithoutJd` 1 — jd_text=None/空时 task_graph 不含 evaluate step 且 bullet_evaluations 是 None 或空 / `TestBulletEvaluationNoBulletsNotError` 2 — helper + workflow 在 highlights 全空时返空 list 不抛 / `TestAffectsPreviewStillFalseUntilConsumed` 1 — `affects_preview("evaluate_bullet_jd_match")` 仍为 False / `TestBulletEvaluationOldPathUnchanged` 2 — 老路径不含 bullet_evaluations 字段 + sections 字节级一致); baseline `test_with_function_calling_adds_evaluate_bullet` 仍通过 (FC=True + has_jd=True 任务图含 evaluate step), `test_tools_used_only_lists_effective_tools` 仍通过 (retrieve_evidence 是唯一 affects_preview=True 工具, evaluate_bullet_jd_match 跑过也不列); 全量 **530 passed + 0 skipped**; **不挂 pre-push hook**; **不引入新 LLM 调用 / 不引入新依赖**; **不破老路径字节级一致** — `enable_agent_workflow=False` 路径不含 `bullet_evaluations` 字段
- **R5-C Phase 1 起：eval request_id 关联 + fallback taxonomy 锁死** —— 来自 `.harness/docs/round5-c-agent-capability-spec.md` §2 实施;`scripts/evaluate_agent_workflow.py` 新增 `_extract_request_id_from_preview(preview)` 优先读 `preview["agent_summary"]["request_id"]` 替代脆弱的 JSONL 末尾反推 + `_extract_tools_used_from_preview(preview)` 优先读 agent_summary.tools_used(spec §2.1);新增 5 类 fallback_category 常量(`FALLBACK_NONE="none"` / `FALLBACK_LLM_DISABLED="llm_disabled_fallback"` / `FALLBACK_TOOL_ERROR="tool_error_fallback"` / `FALLBACK_SCHEMA_RETRY="schema_retry_fallback"` / `FALLBACK_WORKFLOW_ABORT="workflow_abort_fallback"`) + `_classify_fallback_category(preview, llm_enabled, enable_function_calling, enable_agent_workflow, error_type)` 按优先级分类;`_short_request_id(rid)` 只截前 4 字符(报告不输出完整 uuid, spec §2.4 隐私);报告 `AI岗位JD库_agent_eval报告.md` 新增 "6.1 fallback taxonomy 摘要" 段含全局分布表;不挂 pre-push hook(spec §12 #3 默认手动脚本);不修改既有 match_score / score_thresholds.py / match_golden_targets.py / replay_agent_trace.py(spec §2.4)
- **R5-C Phase 5 起：replay 增强 + 文档同步锁死** —— 来自 `.harness/docs/round5-c-agent-capability-spec.md` §6 实施;`scripts/replay_agent_trace.py` 新增 `summarize_fallback(events)` 推断 fallback category(`none` / `tool_error_fallback` / `unknown`, trace 信号不足时归 unknown 提示 caller 查 agent_summary)+ `cross_validate_tools_used(events, expected_tools)` 对账 caller 提供的 agent_summary.tools_used, 输出 `{expected, observed, matched, missing, unexpected, status}` 状态(`ok` / `missing` / `unexpected` / `empty`)+ `render_tools_cross_validation(cross)` 只输出工具名字符串不接触 args / JD / bullet(spec §6.2 隐私);`render_markdown` 默认追加 `## Fallback Summary` 段(基于 trace status/error_type 推断 category, error_steps 计数 + tool_errors 列表只含 step/tool/error_type);`--tools-used <csv>` CLI 参数触发 `## Tools Cross-Validation (R5-C Phase 5)` 段,不传则不显示(向后兼容);`_observed_tools(events)` 严格只接受 str 类型工具名(非 str / None / 空字符串统一跳过, 防止 events 脏数据污染交叉验证);**15 个 R5-C Phase 5 新 pytest** = `tests/test_agent_trace_replay.py` 3 `TestReplayFallbackSummary`(none_when_no_errors / tool_error_when_status_error / markdown_contains_fallback_summary_section)+ 7 `TestReplayToolsCrossValidation`(ok / missing / unexpected / empty / render_only_outputs_safe_fields / cli_with_tools_used_outputs / cli_without_tools_used_hides)+ 2 `TestReplayPiiSafetyR5C`(fallback_summary_does_not_leak_raw_content / full_render_with_both_features_keeps_pii_safe)+ 2 `TestReplayRobustnessR5C`(cross_validate_handles_malformed_events / summarize_fallback_handles_malformed_events)+ 旧 R5-A Phase 2 11 case baseline 不破坏;`evaluate_agent_workflow.py` 不动(spec §6.3 "不修改既有");**不挂 pre-push hook**;**不引入新 LLM 调用 / 不引入新依赖**(纯 stdlib + dataclass);ROADMAP / README / AGENTS 三方同步 baseline 487 → 530 → 544;**老路径不变**(`enable_agent_workflow=False` 字节级一致,replay markdown 默认输出格式向后兼容)
- **R5-D Phase 1 起：eval run mode CLI 锁死** —— 来自 `.harness/docs/round5-d-llm-eval-spec.md` §3 实施;`scripts/evaluate_agent_workflow.py` 新增 `_resolve_eval_mode(mode, llm_enabled)` 纯函数决策 mode 解析(不读 env var,只根据入参)+ 3 类 mode 常量(`MODE_OFFLINE="offline"` 默认 / `MODE_LIVE="live"` / `MODE_AUTO="auto"`) + argparse 加 `--mode` / `--output` CLI 参数;**live mode + llm_enabled=False → RuntimeError**,错误信息**绝不**包含 key 值或 env var 名(spec §3.4 + R5-D §6.4 隐私边界;`TestEvalModeNoKeyLeak` 2 case 锁);`--output <path>` 覆盖默认报告路径(monkeypatch 隔离);`write_report` 加 requested_mode / resolved_mode 入参,报告头标注 mode 用于审计区分 offline/live 报告;**10 个 R5-D Phase 1 新 pytest** = `tests/test_agent_eval.py` `TestEvalModeResolve` 5(offline 总是返 offline / live + 无 key → RuntimeError / live + 有 key → live / auto + 有 key → live / auto + 无 key → offline / 非法 mode → ValueError) + `TestEvalModeNoKeyLeak` 2(live 错误信息不含 api_key 值 / 不含 env var 名) + `TestEvalCliOutput` 3(`--output` 覆盖 / 不传默认路径不变 / 默认 mode 报告头部含 `> Eval mode: **offline**` + `(requested: `offline`)` 标注);547 → 557(Phase 1 +10,Phase 2 round 收尾时统一算入活跃基线 568);**不挂 pre-push hook**(spec §12 #3 默认手动脚本);**不引入新 LLM 调用 / 不引入新依赖**(纯 stdlib);**不破老路径字节级一致** — `enable_agent_workflow=False` 路径不含 mode 元信息以外的额外字段
- **R5-D Phase 2 起：live/offline 报告元信息锁死** —— 来自 `.harness/docs/round5-d-llm-eval-spec.md` §4 实施;`scripts/evaluate_agent_workflow.py` 新增 `_get_llm_eval_config(llm_enabled, resolved_mode) -> dict` 纯函数 helper(返回 4 字段:`llm_mode` / `llm_enabled` / `llm_model` / `llm_base_url_host`),内部用 `urllib.parse.urlparse(...).hostname` 抽 base_url host 部分只输出 host(不含 scheme/path/query/fragment),env var 读 `LLM_MODEL` / `LLM_BASE_URL`,空时回落到 helper 内置 `_DEFAULT_BASE_URL_EVAL` / `_DEFAULT_MODEL_EVAL`(跟 `core/llm_rewriter.py` DEFAULT 同步);**隐私边界**(spec §4.4 + AGENTS.md 隐私边界)— helper **绝不**读 / 写 / 打印 `LLM_API_KEY`(即使 env 设 sentinel key 也不读),`base_url_host` 只展示 host 部分防止 endpoint 路径泄露;`write_report` 加 `llm_eval_config: Optional[dict]` 入参,报告头部新增 `## 0、LLM 元信息 (R5-D Phase 2)` 章节 4 行表格展示 4 字段 + 隐私边界注释(`API key 类凭据` 字面量,不直接引用 env var 名);章节顺序:LLM 元信息章节放在 `Eval set 概览` 之前让审计一眼看到本次跑的 LLM 配置;**11 个 R5-D Phase 2 新 pytest** = `tests/test_agent_eval.py` `TestEvalLlmMetadataHelper` 8(helper 单元:4 字段返回 / live_mode 透传 / 不读 api_key 验证 sentinel 不出现在输出 / base_url 只抽 host 3 个 case 分别覆盖 path/query/extract / model 读 env / 空字符串回落 default) + `TestEvalReportIncludesLlmMetadata` 1(`write_report` 直接调 + 构造最小输入,验证报告含 `## 0、` 章节 + 4 字段名 + 章节在 `## 一、Eval set 概览` 之前) + `TestEvalReportDoesNotIncludeApiKey` 1(sentinel env → helper 输出 + write_report 渲染后报告双重验证:不含 sentinel 值 + 不含 `LLM_API_KEY` 字面量) + `TestEvalReportBaseUrlHostHidesPathAndQuery` 1(env 设 `https://api.example.com/v1/internal/chat?secret=TOPSECRET` → helper 抽出 `api.example.com` + 报告渲染后不含 path/query/secret 字符串);557 → **568 全绿**(557 baseline + 11 R5-D Phase 2 新增,557 老测试零回退);**不挂 pre-push hook**(spec §12 #3 默认手动脚本);**不引入新 LLM 调用 / 不引入新依赖**(纯 stdlib `urllib.parse`);**不破老路径字节级一致** — `write_report` 不传 `llm_eval_config` 时跳过 LLM 元信息章节(向后兼容);测试策略:Phase 2 集成测试**不跑完整 main()**(避免设 `LLM_API_KEY` 触发 fc=F/aw=F 老路径 30s/sample 慢路径副作用),改用直接调 `write_report()` 传构造输入,精准聚焦章节渲染逻辑
- **R5-D Phase 3 起：rewrite impact 指标锁死** —— 来自 `.harness/docs/round5-d-llm-eval-spec.md` §3 实施;`scripts/evaluate_agent_workflow.py` 新增 2 helper:`_extract_project_highlights(preview: dict) -> list[str]`(从 `preview["sections"][i].content.projects[j].content.highlights` 扁平抽出所有 project 高亮,防御性处理 missing projects / no project_group / malformed preview / 非 str 元素,统一返 `[]`)+ `_summarize_rewrite_impact(before: list[str], after: list[str]) -> dict`(纯函数返回 5 数字字段:`rewrite_changed_count` / `rewrite_total` / `rewrite_changed_rate` / `avg_len_before` / `avg_len_after`,`changed_rate = changed_count / total`(total=len(before),空列表 → 全 0),**返回 dict 绝不存任何 bullet 原文**);`evaluate_one` 新增 `baseline_highlights: Optional[list[str]] = None` kwarg(同 jd baseline combo 的 highlights 作 before,内部用 `after` 作 `before` 兜底保证 baseline combo 也产非零 total/avg),返回 row 新增 5 spec 字段 + 内部 `_after_highlights`(前缀 `_` 供 main() 缓存 baseline_combo 的 highlights 给后续 3 combo 复用,缓存完即 strip 不入 row/report);`main()` 4 combo 循环里 baseline combo (combo_idx=0) 缓存 `row["_after_highlights"]` 到 `baseline_highlights_by_jd: dict[str, list[str]]`,后续 3 combo 把缓存值透传给 `evaluate_one` 作 `baseline_highlights`;`compute_metrics` 按 combo 聚合 `rewrite_summary`(5 字段:`avg_rewrite_changed_rate` / `avg_len_before` / `avg_len_after` / `total_changed` / `total_counted`,用 `total_counted` 加权避免 baseline combo 自身拉低指标);`write_report` 新增 `### 7.1 rewrite impact 摘要 (R5-D Phase 3)` 章节展示 4 行 5 列表格 + 隐私边界注释("只展示 changed 计数 / 比例 / 平均长度,**不展示** 任何 bullet 原文"),section 四 每个 JD 工具摘要表新增 `rewrite_rate` 列(`changed/total (X%)`,total=0 时显 `—`);**隐私边界**(spec §3 + AGENTS.md)— `ToolResult` / `agent_summary` / `JSONL trace` / `agent_eval report` 四层链路**绝不**存 / 写 / 展示 bullet 原文,只展示计数 / 比例 / 平均长度(字符数);`output_dir=None` 老路径 `enable_agent_workflow=False` 完全无 rewrite_impact 数据流入(纯 stdlib 字符串处理);offline 模式 4 combo 均应接近 0% rate / avg_len_before ≈ avg_len_after(LLM 关闭走原文 fallback,稳定 0 改写),live 模式 AW 路径应有显著改动率;**16 个 R5-D Phase 3 新 pytest** = `tests/test_agent_eval.py` `TestRewriteImpactCountsChangedBulletsWithoutStoringText` 5(基本 partial changed / 全 unchanged / 全 changed / 空列表 5 字段全 0 / after 比 before 短 zip 截断,每个 case 同步验证 impact dict 不含 bullet 原文)+ `TestExtractProjectHighlightsHandlesMissingProjects` 5(正常嵌套 preview / 缺 projects 字段 / 无 project_group section / highlights 全空 / 多种 malformed preview 输入(None / 非 dict / sections 非 list / projects 非 list / project.content 非 dict / highlights 非 list / 非 str 元素过滤))+ `TestEvalReportContainsRewriteImpactSummary` 2(跑 main() 验证报告含 `rewrite impact` 章节标题 + `rewrite_changed_rate` 字段名 + 至少一个 `%`;evaluate_one 返回 row 必含 5 spec 字段且 baseline combo changed=0)+ `TestEvalReportDoesNotLeakBulletText` 4(helper 输出 dict 不含 sentinel bullet / 真实材料 bullet;设置校验 _extract 确实返回 sentinel;write_report 直接调传 sentinel 数据渲染后报告不含 sentinel;**跑完整 main() 后报告不含 _extract_project_highlights 从真实 materials.json preview 抽出的前 3 条 highlight 文本**(抽样校验 3 条均 >= 10 字符避免误报));568 → **584 全绿**(568 baseline + 16 R5-D Phase 3 新增,568 老测试零回退);**不挂 pre-push hook**(spec §12 #3 默认手动脚本);**不引入新 LLM 调用 / 不引入新依赖**(纯 stdlib 字符串处理);**不破老路径字节级一致** — `_after_highlights` 字段在入 `all_rows` 前 strip 掉,report 不展示该字段;**不破既有 write_report 测试** — metrics 缺 `rewrite_summary` 时走 `.get(..., {})` 兜底返 0.0
- **R5-E Phase 1 起：prompt versioning + 默认字节级稳定锁死** —— 来自 `.harness/docs/round5-e-prompt-optimization-spec.md` §3 Phase 1 实施;`backend/core/llm_rewriter.py` 新增 `PROMPT_VERSION_BASELINE = "v2-baseline"` + `PROMPT_VERSIONS: dict[str, str]` 4 个 key (v2-baseline / v3-priority / v4-counterexample / v5-minimal) + `SYSTEM_PROMPT_V3_PRIORITY` (P0-P4 优先级链) + `SYSTEM_PROMPT_V4_COUNTEREXAMPLE` (v3 + 4 条反例) + `SYSTEM_PROMPT_V5_MINIMAL` (≈5 句话极简版, 字符数 < v2-baseline 60%); `_resolve_prompt_version(prompt_version: str | None) -> str` 纯函数 (None / 空字符串解析为 v2-baseline, 未知 key 抛 ValueError **错误信息不含 prompt 正文**); `_select_system_prompt(prompt_version, evidence_summary)` 选择 base prompt + 按现有规则追加 `EVIDENCE_CONSTRAINT_SUFFIX`; `_build_request_payload` / `_call_with_retry` / `_call_with_agent_loop` / `rewrite_highlights` / `generator.build_sections` / `generator.preview_resume` / `generator.generate_resume_docx` / `agent_workflow.run_agent_workflow` 整链加 `prompt_version: str | None = None` 参数(默认 None → v2-baseline 字节级一致,旧路径完全不变);`PreviewRequest` / `GenerateRequest` 加 `prompt_version: str | None = None` 字段(在 `external_resume_text` 之后, 保持既有字段顺序兼容);**31 个 R5-E Phase 1 新 pytest** = `tests/test_prompt_versioning.py` (TestResolvePromptVersion 4 / TestSelectSystemPrompt 5 / TestBuildRequestPayloadBytewiseStable 5 — 含 default_payload_equals_system_prompt 字节级锁 + explicit_v2_baseline_matches_default 锁 / TestRewriteHighlightsPassthrough 4 / TestApiModels 6 — 含 prompt_version_after_external_resume_text 字段顺序锁 / TestWorkflowPassthrough 2 / TestPrivacyGuarantee 2 — error_message_no_prompt_body 锁 / TestPromptVersionRegistry 3);**默认路径不变** — `prompt_version=None` 时 system prompt 字符串等于当前 `SYSTEM_PROMPT`(字节级锁);`v2-baseline` 内容 = 当前 `SYSTEM_PROMPT`, 切到 winner 必须显式传 `prompt_version="<winner>"`; **不挂 pre-push hook**; **不引入新 LLM 调用 / 不引入新依赖**; `enable_agent_workflow=False` / `enable_function_calling=False` / `session_id=None` / `evidence=None` / `external_resume_text=None` 老路径字节级一致, 596 老测试零回退
- **R5-E Phase 2 起：Prompt A/B 评测脚本 + 隐私安全锁死** —— 来自 `.harness/docs/round5-e-prompt-optimization-spec.md` §3 Phase 2 实施;`scripts/evaluate_prompt_versions.py` 新建, 复用 R5-D `scripts/evaluate_agent_workflow.py` 的 6 个 helper (`load_eval_set` / `_resolve_eval_mode` / `_get_llm_eval_config` / `_extract_project_highlights` / `_percentile` / `_check_pii_safe`) + 5 类 fallback_category 常量(`none` / `llm_disabled_fallback` / `tool_error_fallback` / `schema_retry_fallback` / `workflow_abort_fallback`)+ 4 个 PROMPT_VERSION_DESCRIPTIONS (不展示 prompt 正文, 只展示 version key + 定位 + 字符数);CLI 参数 `--mode {offline,live,auto}` 默认 offline + `--versions <csv>` 默认 4 个全跑 + `--runs-per-version` 默认 1 + `--output` 默认 `AI岗位JD库_prompt_ab报告.md` + `--judge {off,on}` 默认 off + `--judge-model`;**固定 FC=T + AW=T, 只变 prompt_version** — 跟 R5-D 评测 4 开关对照解耦, 报告回答"同一 workflow 下哪个 prompt 更好"而非"workflow 开关是否更好";live mode + llm_enabled=False → RuntimeError, 错误信息**绝不**含 key 值或 env var 名(沿用 R5-D Phase 1 `TestEvalModeNoKeyLeak` 模式);每条样本只存数字和短枚举 — `prompt_version` / `jd_id` / `role` / `run_index` / `schema_pass` / `fallback_used` / `fallback_category` / `latency_ms` / `rewrite_changed_rate` / `avg_len_after` / `tier_required_hit_rate` / `pii_safe`;**隐私边界** (spec §3 + AGENTS.md) — 报告**绝不**存 / 写 / 展示完整 prompt 正文 / JD 原文 / bullet 原文 / 改写后 bullet / LLM response 原文 / API key / 含敏感信息的 base_url path; `ToolResult` / `agent_summary` / `JSONL trace` / `agent_eval report` 四层链路遵循同一边界;PII scan 走 `_check_pii_safe` (placeholder 白名单 11 位手机号 / email 模式);**28 个 R5-E Phase 2 新 pytest** = `tests/test_prompt_eval.py` (TestPromptEvalHelpers 8 — `_is_schema_valid` + `_classify_fallback` 8 case / TestEvaluateOne 3 — offline path / unknown version 不泄漏 / 4 version 都跑通 / TestComputeMetrics 2 / TestMainOfflineReport 9 — offline 报告生成 / 不含 prompt+JD+bullet / runs_per_version / versions 子集 / unknown version 拒绝 / judge off 默认不调 LLM / 4 version 全跑 / TestVersionRegistry 3 — registry 跟 DEFAULT_VERSIONS 一致 + baseline=v2-baseline / TestPrivacyBoundary 3 — PII 自检通过 + 无 sentinel JD + 无 sentinel bullet); 627 → 655 baseline + 28 R5-E Phase 2 新增, 627 老测试零回退;**不挂 pre-push hook** (spec §12 #3 默认手动脚本);**不引入新 LLM 调用 / 不引入新依赖** (纯 stdlib);**不破老路径字节级一致** — `enable_agent_workflow=False` 完全无 rewrite_impact / judge 字段流入;**不修改** `evaluate_agent_workflow.py` / `match_score` / `score_thresholds.py` / `match_golden_targets.py` / `replay_agent_trace.py`
- **R5-E Phase 3 起：可选 LLM-as-Judge 锁死** —— 来自 `.harness/docs/round5-e-prompt-optimization-spec.md` §3 Phase 3 实施;`scripts/evaluate_prompt_versions.py` 新增 judge helper 三件套: `_validate_judge_payload(parsed)` 纯函数校验 schema (quality_score ∈ {1..5} / hallucination ∈ {0,1} / tier_required_hit ∈ {0,1}, 显式排除 bool 是 int 子类, 只校验数字不接触 reasoning 等额外字段) + `_call_judge(bullet_before, bullet_after, evidence_summary, jd_focus, model, base_url, api_key, timeout_sec)` 用 stdlib urllib POST 到 `/chat/completions` (跟 `core.llm_rewriter._http_post_json` 同源), **不 retry** (mock `urllib.request.urlopen` 验证只调 1 次), 网络/超时/HTTPError/JSONDecodeError/schema 错误**一律**返 `(None, True)` 不抛 (隐私: 错误不返回响应原文) + `_summarize_judge_metrics(rows)` 聚合 5 字段 (judge_quality_score_avg / hallucination_rate / tier_required_hit_rate / judge_error_count / judge_evaluated_count, 空 rows 全 0, error 行不计入 evaluated);`evaluate_one` 新增 4 kwargs (`judge_enabled` / `judge_model` / `judge_api_key` / `judge_base_url`) + 4 行输出字段 (`judge_quality_score` / `judge_hallucination` / `judge_tier_required_hit` / `judge_error`), judge_enabled=False 或 judge_api_key 空时**绝不**发起 HTTP (`assert mock_urlopen.assert_not_called()` 锁);`compute_metrics` 每 version 多 4 字段 (`judge_quality_score_avg` / `hallucination_rate` / `tier_required_hit_rate_judge` / `judge_error_count` / `judge_evaluated_count` 5 个实际, judge_evaluated=0 时全 0 避免除零) + 全局 `judge_total_errors` / `judge_total_evaluated`;`write_report` 加 `judge_requested` 参数 (区分用户意图 vs 实际生效, offline 模式强制 disabled 时报告头部明示 "offline mode forces judge disabled"), By Version 指标表加 4 列 (`judge_qs_avg` / `hallucination_rate` / `tier_hit_rate(j)` / `judge_err`, judge_evaluated=0 显示 `—` 区分 0 评分), 新增 `## 5、Judge 摘要 (R5-E Phase 3)` 段 (judge=off 时写"默认关闭" / offline 模式时写"强制 disabled" / live + judge=on 时输出全局 + per-version 表, winner 建议用 composite = qs - halluc*5 综合分);**隐私边界** (spec §3 Phase 3 + AGENTS.md) — `ToolResult` / `agent_summary` / `JSONL trace` / `agent_eval report` 四层链路**绝不**存 / 写 / 展示 judge reasoning / chain-of-thought / 原 bullet / 改写后 bullet / JD 原文; judge 失败 (网络 / JSON / schema / 超时) **不阻断** 主报告生成 (`judge_error_count +1` 而非 raise); `main()` 加 `judge_requested` 标志 + 强制规则: **offline 模式下 judge 强制 disabled** (即使 `--judge on` 也不发 HTTP, 报告头部明示, 防止评测脚本意外产生真实网络调用); judge api_key 走 env `LLM_API_KEY`, judge base_url 走 env `LLM_BASE_URL` 或回落 `_DEFAULT_JUDGE_BASE_URL` (跟 `core.llm_rewriter.DEFAULT_BASE_URL` 同源); judge model 优先 `--judge-model` > env `LLM_JUDGE_MODEL` > env `LLM_MODEL`; 28 个 R5-E Phase 3 新 pytest = `tests/test_prompt_eval.py` `TestJudgeSchemaValidation` 8 (quality_score 1-5 / hallucination 0/1 / tier_required_hit 0/1 / bool 拒绝 / 非 dict 拒绝 / 缺字段拒绝 / extra 字段如 reasoning 被忽略) + `TestJudgeCallRobustness` 5 (网络错误 / JSON 错误 / schema 错误 / 合法 schema / 不 retry 只调 1 次) + `TestJudgeSummarizeMetrics` 3 (空 rows / 5 字段聚合 / 全 error 时 rates 全 0) + `TestJudgeEvaluateOneIntegration` 5 (默认 off 全 None / judge=on 无 key 不发 HTTP / judge=on 有 key 调 urlopen 1 次 / 网络错误 judge_error=True 字段空 / row 序列化不含 sentinel bullet 原文) + `TestJudgeMainOfflineAndReport` 7 (offline + --judge on 不调 urlopen / 报告头部明示 offline 强制 disabled / 默认 judge=off 不发 HTTP / 报告不含 judge reasoning sentinel / By Version 表含 judge 列 / Judge 段 judge=off 默认显示 / compute_metrics 含全局 judge_total_errors 等); 655 → **683 全绿** (655 baseline + 28 R5-E Phase 3 新增, 655 老测试零回退); **不挂 pre-push hook** (spec §12 #3 默认手动脚本); **不引入新 LLM 调用 / 不引入新依赖** (纯 stdlib urllib + json + dataclass); **不破老路径字节级一致** — `--judge off` (默认) 路径不含 judge 字段以外的额外副作用, judge=off 跑 main() 时 `mock_urlopen.assert_not_called()` 通过; **不挂 pre-push**; **不修改** `evaluate_agent_workflow.py` / `match_score` / `llm_rewriter` / `agent_workflow`
- **R5-E Phase 4 起：默认 prompt 不切换 + winner 决策留 R5-E closeout / R5-F 锁死** —— R5-E 收尾阶段明确:`PROMPT_VERSION_BASELINE` 仍为 `"v2-baseline"`, `PROMPT_VERSIONS["v2-baseline"]` 仍指向当前 `SYSTEM_PROMPT` 字符串(字节级锁), **不**把 baseline 改到 winner(无 winner 数据, 即便有也不在本轮切);`SYSTEM_PROMPT` 默认内容**不**改 — few-shot 示例 / 7 条硬性约束 / evidence 第 8 条约束保持 R3-P + R5-A Phase 3 状态; `PreviewRequest` / `GenerateRequest` 不加 `prompt_version` 之外的 selector 字段; 前端**不**新增 prompt selector 组件 — 当前 6 role 单选 + 8 模板单选是用户唯一可控的 UI 入口; live 报告 (含真实 LLM 调用) **不**自动入库 — `AI岗位JD库_prompt_ab报告_live.md` 需用户手动跑 `--mode live --judge on` 且入库前**人工检查 4 条隐私边界**(用公开脱敏 `materials.json` / 用公开 `AI岗位JD库_v4_intern.json` / eval set 来自内置 12 JD 不混入真实投递 JD / `LLM_BASE_URL` 不含内部路径或敏感 query), 任一不满足**绝对不提交**; R5-E 收尾后**下一轮候选二选一** — (a) R5-E closeout / R5-F prompt rollout 基于 live A/B 报告切换默认 (保留 v2-baseline 显式回退); (b) R5-F embedding RAG 升级 evidence retrieval (若 live 报告显示 hallucination 仍高, 优先此路径不切 prompt)
- 前端类型检查：`cd frontend && npx vue-tsc --noEmit` 必须 0 error
- 前端构建：`cd frontend && npm run build` 必须成功
- **新增行为必须有验证**（详见 `.harness/docs/dev-workflow.md`）：
  - 后端：手测对应 API；可补充 `pytest` 用例
  - 前端：手测 UI；类型检查 + 构建为底线
- **bugfix 闭环**:
  - 复盘本轮 diff,逐文件过 — 签名/docstring/阈值是否一致
  - 修一个 bug 必须加 ≥1 个回归测试
  - 跑全量绿 + 前端构建成功才 commit

## PR & commit conventions

- 从 `main` 拉特性分支（如 `feat/round2-jd` / `fix/<scope>` / `chore/<scope>`）
- **永远不要直接 push 到 `main`** — 用 PR/MR 合并
- 提交信息遵循 conventional commits（`feat:` / `fix:` / `docs:` / `refactor:` / `chore:` / `test:`）
- 范围限定：消息中标注 round 编号（如 `feat(round2#2): JD 解析` / `fix(round3#a): bug hunt`）
- **bugfix commit 必须说明**:
  - 哪个 bug (Bug A/B/C 编号)
  - 触发条件 (e.g. score=50-59 时 UI 冲突)
  - 修复路径 (改哪几个文件 + 阈值/签名/注释)
  - 回归测试覆盖 (e.g. +6 TestBugfix*)

## Privacy & deploy

- **本地单用户**：不要加账号系统、不要把后端暴露公网
- `backend/data/materials.json` 是公开仓库版（已脱敏）；真实数据走 `backend/data/_private_backup.json`（`.gitignore`）
- clone 后想用自己的数据：`cp backend/data/_private_backup.json backend/data/materials.json`
- 生成产物 `backend/output/`、日志 `backend/logs/` 都在 `.gitignore` 内，**不要 commit**

## Security

- **不要 commit 任何真实个人信息**（姓名 / 手机 / 邮箱 / 学校 / 公司）
- 新增依赖前确认 license 兼容（MIT / Apache-2.0 / BSD 优先）
- 不要在日志里输出完整请求体（可能含 PII）— 见 `.harness/docs/privacy-deploy.md`

---

> 详细的开发流程、角色分工、素材库结构约定见 `.harness/docs/`。
> Agent 团队分工（orchestrator + developer + tester）见 `.harness/agent.md`。
